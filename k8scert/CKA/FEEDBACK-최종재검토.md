# CKA 최종 재검토 피드백 (개선 후)

> 생성: 2026-06-13 (스크린샷+스토리라인+보안도구 실측 반영 후) · 파일당 1 에이전트 병렬 재검토

- 단독합격 **20/25** · 평균 가독성 **4** · 스토리라인 **4.1**
- 스크린샷 활용: 부분 19, 양호 5, 참조과다 1
- 남은 부족분 **200** (HIGH 60) · HIGH 카테고리: 스크린샷부족 27, 실습재현불가 14, 구조 7, 정확성오류 6, 맥락점프 4, 이해난이도 1, 스토리라인누락 1

| 파일 | 합격 | 가독성 | 스토리 | 스샷 | H/M/L |
|:--|:--:|:--:|:--:|:--:|:--:|
| 01-concepts.md | O | 4 | 4 | 부분 | 4/4/2 |
| 02-examples.md | O | 4 | 5 | 부분 | 4/4/2 |
| 03-exam-questions.md | O | 4 | 4 | 부분 | 2/3/2 |
| 04-tart-infra-practice.md | X | 4 | 5 | 부분 | 2/3/1 |
| 05-supplement.md | X | 4 | 4 | 참조과다 | 2/4/2 |
| daily/day01.md | O | 4 | 5 | 양호 | 2/3/2 |
| daily/day02.md | X | 4 | 3 | 부분 | 4/4/2 |
| daily/day03.md | O | 4 | 5 | 양호 | 0/2/3 |
| daily/day04.md | O | 4 | 4 | 부분 | 2/4/2 |
| daily/day05.md | O | 4 | 5 | 부분 | 2/3/3 |
| daily/day06.md | O | 4 | 5 | 양호 | 3/4/2 |
| daily/day07.md | O | 4 | 4 | 부분 | 2/3/2 |
| daily/day08.md | O | 4 | 4 | 부분 | 3/3/2 |
| daily/day09.md | O | 4 | 4 | 부분 | 2/3/2 |
| daily/day10.md | X | 4 | 4 | 부분 | 3/4/2 |
| daily/day11.md | O | 4 | 4 | 부분 | 0/3/4 |
| daily/day12.md | O | 4 | 3 | 부분 | 3/4/2 |
| daily/day13.md | O | 4 | 5 | 양호 | 1/3/2 |
| daily/day14.md | O | 4 | 4 | 부분 | 3/3/2 |
| daily/day15.md | O | 4 | 4 | 부분 | 2/3/2 |
| daily/day16.md | O | 4 | 4 | 부분 | 3/3/2 |
| daily/day17.md | O | 4 | 4 | 부분 | 3/3/2 |
| daily/day18.md | O | 4 | 4 | 부분 | 2/5/3 |
| daily/day19.md | X | 4 | 3 | 부분 | 3/4/2 |
| daily/day20.md | O | 4 | 3 | 양호 | 3/4/2 |

---

### 01-concepts.md
- **HIGH** `스크린샷부족` _섹션 2(스케줄링), 3(NetworkPolicy/CoreDNS/CNI), 4(PV/PVC/StorageClass), 5(Pod 상태별 진단) 전 구간_ — 실제 터미널 스크린샷 11개 대비 '예시(참조)' blockquote 74개로 비율이 87%에 달한다. Taint, nodeSelector, etcd 백업/복구, StorageClass, Service 연결 확인, crictl ps 등 CKA 시험에서 반드시 손으로 확인해야 하는 명령 출력이 전부 참조 blockquote로 처리되어, 학생이 실제 화면이 어떻게 나오는지 확인할 수 없다. → 클러스터 가동 후 Taint, etcd snapshot status, kubectl get pv/pvc, kubectl get endpoints, crictl ps 등 핵심 출력을 최소 15개 추가 캡처하여 images/에 저장하고 해당 참조 blockquote를 교체한다.
- **HIGH** `구조` _## 5. Troubleshooting, 섹션 번호_ — 5.4가 존재하지 않는다. 5.1 -> 5.2 -> 5.3 -> 5.5로 번호가 건너뛴다. 목차와 본문 모두 5.4가 누락되어 있어 내용이 빠진 것인지 오타인지 학생이 판단하기 어렵다. → 5.5를 5.4로 수정하거나, 의도적으로 비워둔 경우 '5.4 Control Plane 로그 수집'처럼 주제를 명시하고 내용을 채운다.
- **HIGH** `이해난이도` _섹션 2.2 Pod 스케줄링 / kube-scheduler 내부 동작 (PodTopologySpread 설명)_ — PodTopologySpread 필터 플러그인을 설명하면서 maxSkew 개념만 서술하고, 실제 YAML 예시(topologySpreadConstraints 블록)가 전혀 없다. CKA에서 topologySpreadConstraints는 직접 작성해야 하는 YAML이지만, concepts 파일에 한 줄도 없어 학생이 시험에서 재현할 근거가 없다. → 섹션 2.2에 topologySpreadConstraints YAML 예시(maxSkew, topologyKey, whenUnsatisfiable, labelSelector 포함)를 추가하고 scheduler 내부 PodTopologySpread 플러그인과 연결하는 설명을 덧붙인다.
- **HIGH** `스토리라인누락` _섹션 2(Workloads) 전체_ — liveness/readiness/startup probe 전용 절이 없다. kubelet 내부 동작(130행)에 probe 언급이 있고 CrashLoopBackOff 원인 목록에 '프로브 실패'가 나오지만, probe의 등장 배경(헬스체크 없는 시절의 문제), 3종 probe 차이, httpGet/exec/tcpSocket 예시, initialDelaySeconds/periodSeconds 파라미터 설명이 전혀 없다. CKA 시험에서 빈번하게 출제되는 항목이다. → 2.7절로 'Probe(liveness/readiness/startup)'를 추가하고, 3종 probe의 등장 배경, YAML 예시, 시험 팁(readiness vs liveness 혼동 방지)을 서술한다.
- **MED** `스토리라인누락` _섹션 2(Workloads)_ — ConfigMap과 Secret을 독립 절로 다루지 않는다. Ingress TLS Secret, CoreDNS ConfigMap 등에서 단편적으로 등장하나, 'kubectl create configmap', 'kubectl create secret generic', envFrom/valueFrom 마운트 패턴, Secret 타입 목록이 없다. CKAD 범위와 겹치더라도 CKA에서 ConfigMap/Secret을 이용한 Pod 설정은 기본 문제 유형이다. → 2.7 또는 2.8절로 'ConfigMap과 Secret' 절을 추가하고 생성 명령, envFrom 마운트, volumeMount 방식, 시험 주의사항(base64 인코딩 여부)을 다룬다.
- **MED** `용어비약` _섹션 1.2 kubeadm init, 'Bootstrap token' 설명_ — 'TLS 부트스트래핑'이라는 용어가 kubeadm join 과정(307행)에 처음 등장하나 별도 풀이가 없다. CSR, bootstrap-token Secret 대조 흐름을 짧게라도 설명하지 않으면 학부 3-4학년이 토큰과 인증서의 관계를 이해하기 어렵다. → kubeadm join 절에 'TLS 부트스트래핑 = 초기 토큰으로 신원을 확인한 뒤 정식 클라이언트 인증서를 발급받는 절차'를 한 단락으로 추가한다.
- **MED** `실습재현불가` _섹션 1.2 kubeadm init 전체, 섹션 1.3 업그레이드, 섹션 1.4 etcd 백업/복구_ — kubeadm init/join, 업그레이드, etcd backup/restore 절에 실측 스크린샷이 단 한 장도 없다. 이 주제들은 CKA 25-30점 분량이며 절차의 성공/실패 판단을 출력 화면으로 익혀야 한다. dry-run 출력, kubeadm upgrade plan 출력, snapshot status 테이블, member list 테이블이 모두 참조 blockquote이다. → staging 클러스터에서 kubeadm upgrade plan, etcdctl snapshot save/status, member list 출력을 실측 캡처하여 교체한다(최소 5장).
- **MED** `맥락점프` _섹션 문서 첫머리 (1행~18행)_ — 'alias k=kubectl', 'export do=--dry-run=client -o yaml' 같은 시험 필수 셸 설정이 문서 맨 끝(2769-2773행)에만 있고, 실습 명령 예시에는 적용되지 않는다. 학생이 실습 명령을 따라치다가 'k'가 무엇인지 모른다. CLAUDE.md도 이 설정을 '처음부터 안내'하라고 명시하고 있다. → 학습 목표 바로 아래에 '시험 환경 사전 설정' 블록을 추가하고, 이후 실습 명령에서 'k', '$do' 단축어를 일관 사용한다.
- **LOW** `스크린샷부족` _섹션 5.2 Pod 상태별 진단 (Pending, CrashLoopBackOff, ImagePullBackOff, OOMKilled)_ — 각 Pod 상태별로 'kubectl describe pod' 이벤트 출력이 어떻게 생겼는지 스크린샷이 없고 전부 참조 blockquote이다. 특히 CrashLoopBackOff의 Events 섹션과 OOMKilled reason 필드는 처음 보는 학생에게 낯설다. → dev 클러스터에서 리소스 초과 Pod, 존재하지 않는 이미지 Pod를 생성하여 describe 출력을 각 1장씩 캡처한다.
- **LOW** `이해난이도` _섹션 1.4 etcd 복구 절차, 4단계 설명_ — etcd 복구 후 '--data-dir 경로 변경 시 etcd 매니페스트의 hostPath 볼륨 마운트도 수정해야 한다'는 내용이 단순 텍스트로만 나온다. 어떤 필드를 어떻게 바꾸는지 YAML 스니펫이 없어 시험 중 혼동이 생길 수 있다. → etcd.yaml의 volumes.hostPath.path와 --data-dir 플래그를 함께 수정하는 YAML 차이(before/after) 또는 sed 명령 예시를 추가한다.

### 02-examples.md
- **HIGH** `스크린샷부족` _§1.2 검증 블록 3곳 (kubeadm upgrade plan, 중간 버전 스큐, 업그레이드 완료 후 get nodes)_ — 업그레이드 핵심 3개 검증 블록이 모두 '예시(참조)' blockquote로 대체되고 실제 스크린샷이 없다. '우리 클러스터는 전부 v1.31.14로 균일하다'는 주석이 있어 캡처가 가능한 상태임에도 이미지가 없다. → staging 클러스터에서 실제 버전 스큐 중간 상태와 완료 상태를 캡처해 m02-05-upgrade-plan.png, m02-06-upgrade-mid.png, m02-06b-upgrade-done.png로 삽입하라.
- **HIGH** `스크린샷부족` _§3.1 Service 검증 블록 — kubectl get svc, kubectl get endpoints 두 명령_ — get svc와 get endpoints가 '예시(참조)' blockquote로만 제시되고 실제 스크린샷이 없다. Service는 CKA 시험 비중이 높은 항목으로, 학생이 실제 출력 형식을 눈으로 익히지 못한다. → dev 클러스터에 nginx-svc를 배포한 뒤 get svc와 get endpoints를 캡처해 m02-09-svc.png, m02-10-ep.png로 삽입하라.
- **HIGH** `스크린샷부족` _§3.2 Ingress 검증 블록 — kubectl get ingress, kubectl describe ingress_ — Ingress 두 검증 명령이 모두 '예시(참조)'로만 처리됐다. dev 클러스터에 ingress-nginx 컨트롤러가 없다는 이유로 참조로 대체했으나, 컨트롤러 없이도 'get ingress'는 규칙·host·class 컬럼을 출력하므로 ADDRESS가 비어 있는 상태 그대로 캡처 가능하다. → 컨트롤러 없는 dev 환경에서 'kubectl get ingress' 출력을 그대로 캡처하고 '(ADDRESS가 비어 있는 상태 -- 컨트롤러 미설치)' 캡션을 붙여 m02-35-ingress.png로 삽입하라.
- **HIGH** `실습재현불가` _§1.2 전체 업그레이드 절차_ — 업그레이드 절차가 staging 클러스터 기준인지 dev 기준인지 명확하지 않다. 본문 상단 주의문에는 'staging 실습'이라 나와 있지만 명령어는 <node-name> 자리표시자 그대로여서, 학생이 staging에서 실제로 따라 할 때 어떤 노드명을 쓰는지 모른다. → 'staging 클러스터 기준. 노드명은 kubectl get nodes로 확인 후 대입하라. 예: <control-plane-node>=staging-master'처럼 구체적 예시를 단 한 줄이라도 삽입하라.
- **MED** `용어비약` _§2.11 ConfigMap/Secret 검증 블록_ — 검증 블록에 'kubectl exec env-from-pod -- env | grep ...' 명령이 있지만 결과 스크린샷이 전혀 없다. 환경변수가 실제로 어떻게 보이는지(APP_ENV=production 등) 눈으로 확인하는 절차가 빠졌다. → env-from-pod에서 env 명령 실행 결과를 캡처해 m02-42-env.png 등으로 삽입하라. Pod 내부 파일 마운트(cat /etc/app/config/app-config.yaml) 결과도 함께 캡처하면 좋다.
- **MED** `스토리라인누락` _§5 트러블슈팅 치트시트 전체 (2515-2687행)_ — 섹션 5는 kubectl 명령 목록과 표만 있고, 실제 장애 상황에서 어떻게 순서를 따라가는지 스토리가 없다. 또 5.1~5.5 어디에도 스크린샷이 없어(5.5 certs 1개만 예외) 학생이 각 명령의 실제 출력을 본 적 없이 시험에 임해야 한다. → 'Pod가 Pending인 경우 - 이벤트 확인 -> describe -> taint/node 확인'처럼 최소 1개의 단계별 장애 재현 시나리오를 스크린샷과 함께 삽입하라. 5.2 Pod 디버깅에서 CrashLoopBackOff 재현 화면이 특히 유용하다.
- **MED** `맥락점프` _시험 시작 설정 섹션 (2861행)_ — CKA 실기 시험의 가장 첫 번째 동작인 'kubectl config use-context <ctx>'가 언급되지 않는다. 시험은 문제마다 컨텍스트를 전환하도록 요구하는데, 문서 어디에도 이 패턴이 없어 시험 절차에 결정적 구멍이 생긴다. → '시험 시작 시 설정' 섹션에 'kubectl config use-context <ctx> # 문제마다 가장 먼저 실행'을 추가하고, get-contexts로 목록 확인하는 방법도 한 줄 안내하라.
- **MED** `이해난이도` _§1.3 etcd 복구 절차 — 2단계 etcd.yaml 수정 (403-423행)_ — etcd.yaml의 hostPath를 수정하라고만 설명하고 수정 후 실제 반영 확인 스크린샷이 없다. 특히 'watch crictl ps | grep etcd'로 재시작을 기다리는 부분은 실제로 어떻게 보이는지 모르면 학생이 기다려야 할지 알 수 없다. → etcd Static Pod가 재시작되어 Running으로 전환되는 crictl ps 출력을 캡처해 삽입하라. kubectl get pods -A 복구 확인 결과도 캡처하면 완성된다.
- **LOW** `구조` _파일 전체 목차(상단)_ — 파일에 목차가 없다. 2893행 분량의 문서인데 상단에 섹션 링크 목차가 없어 어떤 내용이 있는지 한눈에 파악하기 어렵다. → 파일 상단 '실습 전제' 바로 위에 ## 목차 섹션을 두고 1~6장을 하이퍼링크로 나열하라.
- **LOW** `실습재현불가` _§1.1 사전 준비 (apt 패키지 설치) 전체_ — Ubuntu/Debian 전제로만 작성됐고, 이 저장소의 실제 VM(tart Ubuntu 기반)에서 완전히 동작하는지 검증 스크린샷이 없다. 'containerd 설치(Ubuntu/Debian)'라고 명시돼 있으나 tart VM의 실제 OS 버전(예: Ubuntu 22.04)이 어디에도 적시되지 않아 독자가 환경을 특정하지 못한다. → 사전 준비 첫 줄에 'tart VM OS: Ubuntu 22.04 LTS, arch: arm64'를 명시하고, 실제 설치 완료 후 'dpkg -l kubelet' 등 버전 확인 스크린샷을 1개 이상 추가하라.

### 03-exam-questions.md
- **HIGH** `스크린샷부족` _문제 3(Worker 업그레이드 검증), 5(etcd 복구 검증), 10(HA 조인 검증), 19(Ingress get/describe), 24(TLS Ingress get/describe), 25(PV/PVC Bound 상태·exec df), 27(sidecar 로그 공유), 28(Released->Available PV), 30(CrashLoopBackOff 해결 후 get pod), 32(Pending->Running 전환), 36(error-log.txt cat), 38(DNS nslookup 복구), 39(drain/uncordon 전후 get nodes), 40(cluster-health.txt wc -l)_ — 14개 문제(3·5·10·19·24·25·27·28·30·32·36·38·39·40)의 검증 단계에 실제 스크린샷이 전혀 없고 '예시(시험 정답 형식, 참조)' blockquote만 있다. 이 참조들은 daily 문서를 별도로 열어야 확인 가능하다고 안내하지만, 이 파일 단독으로는 '정답이 이렇게 나온다'는 시각적 증거가 없어 학생이 자신의 출력이 맞는지 판단하기 어렵다. 특히 문제 25(PV Bound), 27(emptyDir 공유), 39(drain 전후 상태 변화)는 상태 변화를 눈으로 확인해야 이해가 되는 문제들이다. → dev/staging 클러스터에서 각 검증 명령을 직접 실행하고 터미널 스크린샷을 캡처해 images/m03-XX-*.png 로 저장한 뒤 ![설명](images/...) 으로 교체한다. 캡처가 어려운 환경이면 최소 '(미캡처)' 표기로 대체하고 참조 blockquote는 제거한다.
- **HIGH** `실습재현불가` _문제 10(HA Control Plane 조인), 문제 26(WaitForFirstConsumer), 문제 40(etcd member list in 종합 점검)_ — 문제 10은 HA 클러스터(control plane 3대)가 필요한데 이 저장소 클러스터는 dev(master+worker1) 구조라 HA 조인 자체를 재현할 수 없다. 파일 상단의 실습 전제 블록에 이 제약이 언급되지 않아 학생이 혼자 시도하다 막힌다. 문제 26은 kubernetes.io/no-provisioner가 동적 프로비저닝을 하지 않음을 주의 블록에서 설명하지만, 실제로 PVC Pending 이후 어떻게 PV를 수동 생성해 바인딩하는지 단계가 빠져 있어 실습이 중간에 끊긴다. → 문제 10 상단에 '이 문제는 HA 클러스터(control plane 3대) 환경이 필요해 이 저장소 dev/staging에서 완전 재현 불가. kubeadm init phase upload-certs 명령 실습만 staging-master에서 확인한다.'를 명시한다. 문제 26은 '이 StorageClass는 정적 프로비저닝 전용이므로 5Gi hostPath PV를 먼저 수동 생성한 뒤 PVC를 만들어야 Bound가 된다'는 추가 단계와 YAML을 풀이에 삽입한다.
- **MED** `맥락점프` _문제 2(Control Plane 업그레이드) 검증 단계 138행_ — get nodes 검증에 스크린샷 1개(m03-03-pods-images, Custom-columns)는 있지만 정작 'controlplane VERSION v1.31.0' 확인용 get nodes 스크린샷은 참조 blockquote로 처리되어 있다. 그 위에 스크린샷이 1개 있으나 그 파일명(m03-03-pods-images)은 Pod 이미지 버전 확인용이므로, 노드 버전 확인 결과를 보여주는 스크린샷이 실질적으로 없다. → dev 클러스터에서 업그레이드 실습 후 'kubectl get nodes' 출력 스크린샷을 캡처해 images/m03-02-nodes-v1.31.png 로 저장하고 138행 참조 blockquote를 교체한다.
- **MED** `스토리라인누락` _문제 19(Ingress), 문제 24(TLS Ingress)_ — Services & Networking 섹션 도입부에 Ingress 등장 배경 설명이 있지만, Ingress Controller(nginx-ingress-controller)를 클러스터에 설치하는 방법이 전혀 언급되지 않는다. 학생이 kubectl apply -f app-ingress.yaml을 실행해도 Ingress Controller가 없으면 ADDRESS 칼럼이 빈 채로 남아 '내가 뭘 잘못했나'고 혼동한다. 이 설치 전제가 두 문제 모두에서 'Ingress Controller가 설치되어 있어야 한다'는 한 줄만으로 처리된다. → 문제 19 풀이 앞에 'dev 클러스터에 nginx Ingress Controller가 없으면 kubectl apply -f https://raw.githubusercontent.com/.../deploy.yaml 으로 먼저 설치한다. 설치 확인: kubectl -n ingress-nginx get pods'를 추가하고 실측 스크린샷 또는 (미캡처) 표기를 붙인다. 문제 24도 동일 처리.
- **MED** `이해난이도` _문제 22(Egress NetworkPolicy), egress 규칙 YAML 1754-1757행_ — 첫 번째 egress 규칙에 'to: 없이 ports만' 있어 '모든 목적지의 53번 포트'를 허용한다고 설명하지만, 학생 입장에서 'to 없이 ports만' 패턴이 왜 모든 목적지가 되는지 NetworkPolicy 파싱 규칙을 모르면 이해하기 어렵다. 또한 두 egress 항목이 OR 관계임을 YAML 들여쓰기 수준으로만 암시하고 명시적으로 설명하지 않는다. → 'egress 항목이 여러 개이면 OR(어느 하나라도 맞으면 허용). 단일 항목 안에서 to+ports는 AND(둘 다 맞아야 허용). 첫 번째 항목처럼 to가 없으면 모든 목적지 IP에 대해 해당 포트만 열린다.'를 핵심 원리 섹션에 2~3문장으로 명시한다.
- **LOW** `구조` _문제 전체 상단 공통 tips 블록_ — 파일 최상단의 공통 시간 절약 팁은 alias/export 설정을 언급하지만 실제 시험 환경에서 이 alias가 미리 설정되어 있는지(또는 학생이 직접 입력해야 하는지) 명시되어 있지 않다. 현재 CKA 시험 환경(killer.sh 기반)은 alias k=kubectl이 기본 제공되지 않아 학생이 먼저 입력해야 하는데 이 사실이 빠져 있다. → '시험 시작 직후 첫 명령으로 alias k=kubectl && export do="--dry-run=client -o yaml" 을 직접 입력해야 한다. 현재 CKA PSI Bridge 환경은 이 alias를 기본 제공하지 않는다.'를 공통 tips 블록에 추가한다.
- **LOW** `정확성오류` _문제 34(kube-scheduler 복구) 풀이 2740-2741행_ — 'cat /etc/kubernetes/manifests/kube-scheduler.yaml' 을 문제 풀이 2단계로 안내하고 있는데, scheduler가 완전히 사라진(매니페스트 삭제) 시나리오에서는 이 파일이 존재하지 않아 cat 명령이 오류를 낸다. 고의 장애 주입 블록(mv /tmp/kube-scheduler.yaml back)에서는 파일이 /tmp에 있다는 것을 알지만, 처음 보는 학생은 파일이 어디 갔는지 모른다. → '매니페스트가 없는 경우(ls /etc/kubernetes/manifests/ 로 먼저 확인): sudo find /tmp /etc/kubernetes -name kube-scheduler.yaml 2>/dev/null 로 위치를 찾고, 원래 경로(/etc/kubernetes/manifests/)로 복사한다.'를 원인 1 해결 단계에 추가한다.

### 04-tart-infra-practice.md
- **HIGH** `스크린샷부족` _Lab 1.2~3.x 전체 검증 단계 (약 32개 '# 출력 예시:' 블록)_ — Labs 1.2~3.10 구간의 수십 개 검증 단계가 '# 출력 예시:' 텍스트 주석으로만 존재하며 실제 ![](images/..) 스크린샷이 한 장도 없다. Lab 1.1만 실사 이미지 2장을 포함하고 그 이후 모든 Storage 이전 랩은 가상 출력이다. → 실제 tart 클러스터에서 각 검증 명령을 실행한 뒤 scripts/capture-shot.sh 로 캡처하여 images/ 폴더에 저장하고 ![](images/...) 형식으로 교체한다.
- **HIGH** `실습재현불가` _Lab 3.4 NodePort 서비스 외부 접근 테스트 - 확인 문제 하단_ — 다른 47개 이상의 랩에는 모두 '확인 문제 풀이' 섹션이 존재하지만 Lab 3.4의 확인 문제 3개(NodePort 전체 노드 접근 가능 이유, 30000-32767 범위 이유, keycloak readinessProbe 역할)에 대한 풀이 섹션이 완전히 누락되어 있다. → Lab 3.4 확인 문제 바로 아래에 '#### 확인 문제 풀이' 섹션을 추가하고 3개 질문에 대한 상세 답변을 작성한다.
- **MED** `정확성오류` _Lab 2.10 Step 1 CronJob 생성 명령_ — kubectl create cronjob date-printer ... -- sh -c "echo 'Current time: $(date)'" 에서 외부 쌍따옴표 안의 $(date)가 CronJob 실행 시가 아닌 명령 입력 시점에 현재 셸에 의해 즉시 확장된다. 결과적으로 모든 CronJob Pod에서 동일한 고정 시각이 출력된다. → 단일 인용부호로 변경: -- sh -c 'echo "Current time: $(date)"' 또는 이스케이프 처리: -- sh -c "echo 'Current time: \$(date)'" 로 수정한다.
- **MED** `스크린샷부족` _Lab 4.3 PV-PVC 바인딩 트러블슈팅 (파일 4267행)_ — storageClassName 불일치 시나리오(4263행)와 용량 부족 시나리오(4267행) 두 곳 모두 images/m04-02-failed-binding.png 하나를 공유한다. 두 장애 원인이 다름에도 동일 이미지가 표시되어 시각적으로 구분이 불가능하다. → 용량 부족 시나리오에서 PVC capacity 초과 상태를 별도 캡처하여 images/m04-03-capacity.png 로 저장하고 4267행의 이미지 참조를 교체한다.
- **MED** `맥락점프` _부록 A tart-infra 주요 접근 정보 표 (서비스 URL 행)_ — 모든 서비스 URL이 http://<node-ip>:3xxxx 플레이스홀더로만 표기되어 있고 실제 IP를 조회하는 방법이 설명되지 않는다. 학생이 처음 환경을 접할 때 IP를 어떻게 확인해야 하는지 알 수 없다. → 표 상단에 'kubectl --context=dev get nodes -o jsonpath="{.items[0].status.addresses[?(@.type=="InternalIP")].address}"' 명령으로 노드 IP를 조회하는 단계를 추가하고 플레이스홀더 대신 조회 방법을 명시한다.
- **LOW** `맥락점프` _Labs 1.3, 1.4, 1.9, 1.10, 5.6, 5.11 SSH 접속 코드 블록_ — Lab 1.2와 5.5의 '실습 전제' 박스에서 ssh platform-master 별칭과 ~/.ssh/tart_k8scert 키를 설명했지만 이 랩들의 코드 블록에는 ssh admin@<platform-master-ip> 플레이스홀더가 남아 있어 일관성이 깨진다. → 해당 코드 블록의 SSH 명령을 ssh platform-master 별칭으로 통일하거나 주석으로 '(또는: ssh platform-master)' 를 병기한다.

### 05-supplement.md
- **HIGH** `스크린샷부족` _Part 1 (1.1~1.5 검증 섹션, 라인 89~912)_ — PSA enforce 거부(403), SecurityContext id/cap 확인, metrics-server kubectl top 등 핵심 실습 출력 213개 중 1개만 실제 스크린샷(m05-01-apiservice.png)이고 나머지는 모두 # 예상 출력: 텍스트 블록이다. CLAUDE.md 4-1 규약 위반 상태가 대부분의 Part 1~4에 걸쳐 남아 있다. → 1.1~1.5의 핵심 검증 명령(kubectl run priv-test 거부, kubectl exec secure-pod -- id, kubectl exec restricted-pod -- cat /proc/1/status | grep Seccomp, kubectl top nodes 등) 최소 각 절당 1~2개를 실제 tart dev/staging 클러스터에서 실행 후 screencapture 캡처하여 images/m05-xx-*.png로 저장하고 ![](images/...) 형태로 교체한다. 비정상 상태 4개(metrics-server False, controller-manager x509 오류, scheduler connection refused, CoreDNS Endpoints none)는 실제 장애 재현이 어려우므로 참조 blockquote를 유지하되, 정상 상태 검증은 반드시 실제 스크린샷으로 대체한다.
- **HIGH** `실습재현불가` _예제 7 StatefulSet + Headless Service (라인 1613~1615)_ — volumeClaimTemplates의 storageClassName: standard가 tart dev/staging 클러스터에 존재하지 않아 PVC가 Pending 상태에 머물고 StatefulSet Pod가 Running이 되지 않는다. 주석으로 경고하고 있으나 학생이 따라하면 실패한다. → tart 클러스터에서 kubectl get sc로 실제 사용 가능한 StorageClass 이름을 확인하고 그 이름으로 교체하거나, 또는 동적 프로비저닝 없이 hostPath PV를 volumeClaimTemplates 대신 직접 volumes로 마운트하는 대안 예제를 별도 제공한다. 실제 실행 가능한 예제여야 학생이 검증 단계를 따라할 수 있다.
- **MED** `정확성오류` _1.2 Sidecar Container와의 차이 섹션 (라인 380)_ — 'sidecar 패턴은 아직 GA(General Availability)가 아닌 버전에서는 출제되지 않을 수 있다'는 표현이 부정확하다. native sidecar(Init Container에 restartPolicy: Always)는 Kubernetes 1.29에서 GA되었고, 현재 시험 환경인 1.30~1.31 기준으로는 이미 GA 기능이다. → 'Kubernetes 1.29에서 GA(General Availability)로 승격되었다. 1.30 이상 시험 환경에서는 정식 기능이나, 문제 출제 빈도는 아직 낮다'로 수정한다.
- **MED** `용어비약` _1.1 PSA 트레이드오프 섹션 (라인 226)_ — OPA/Gatekeeper, Kyverno를 한 문장에서 '외부 Admission Webhook'으로 언급하고 넘어간다. 학부 3~4학년 독자가 이 도구들이 무엇인지 알 수 없다. → 각 도구에 한 줄 설명을 추가한다. 예: 'OPA/Gatekeeper(Open Policy Agent - 정책을 코드(Rego 언어)로 작성하여 Admission Webhook으로 강제하는 도구)나 Kyverno(YAML 기반 정책 엔진, OPA 대비 설정이 단순하다) 같은 외부 Admission Webhook을 추가해야 한다.'
- **MED** `구조` _문서 상단 헤더 (라인 1~13)_ — CLAUDE.md 5조에서 필수로 요구하는 '> 학습 목표 | 도메인: (비중 %) | 예상 소요 시간' 헤더 블록이 없다. 학생이 이 문서가 CKA 시험 도메인의 어느 영역에 해당하는지, 몇 시간이 필요한지 알 수 없다. → 문서 제목 바로 아래에 '> 학습 목표: PSA/SecurityContext/Init Container/PriorityClass/metrics-server 심화 + etcd 백업/업그레이드/NetworkPolicy/RBAC 실전 예제 + 50문항 | 도메인: Cluster Architecture 25%, Workloads & Scheduling 30%, Services & Networking 20%, Storage 15%, Troubleshooting 10% | 예상 소요: 8~12시간' 형태로 헤더를 추가한다.
- **MED** `스크린샷부족` _Part 5 API 요청 처리 흐름 (라인 5211~5219)_ — CLAUDE.md 4-2에서 개념/흐름 그림은 mermaid 흑백 다이어그램을 사용하도록 규정하고 있으나, ASCII 화살표 다이어그램(```...``` 펜스)으로 구현되어 있다. 한글 라벨이 포함되어 있고 정렬이 시각적으로 어긋난다. → CLAUDE.md 규약의 흑백 mermaid 테마 지시자를 포함한 flowchart TD 또는 sequenceDiagram으로 교체한다. kubectl -> apiserver -> controller-manager -> scheduler -> kubelet 단계를 mermaid로 표현하고 캡션 '_그림 1. CKA API 요청 처리 흐름._'을 부여한다.
- **LOW** `맥락점프` _문서 서두 (라인 3~13)_ — 이 문서가 01~04 문서의 어떤 개념을 전제하는지 명시되지 않았다. 'YAML은 시험 환경에서 즉시 사용 가능한 형태로 작성한다'고만 되어 있어, 이 문서를 처음 여는 학생이 어떤 사전지식을 갖추고 왔어야 하는지 알 수 없다. → '이 문서를 읽기 전에 01-concepts.md(핵심 개념), 02-examples.md(YAML 예제), 04-tart-infra-practice.md(tart 클러스터 실습)를 완료해야 한다. 특히 kubectl 기본 조작, RBAC 기본 개념, StorageClass/PV/PVC 관계는 이미 알고 있다고 가정한다'는 전제 문장을 추가한다.
- **LOW** `이해난이도` _예제 7 StatefulSet 내부 동작 (라인 1545~1565)_ — Headless Service 관계를 설명하는 단락 직후(라인 1559)에 'StatefulSet에 적합하지 않은 요구사항' 단락이 등장하는데, 이 단락은 실제로는 Deployment의 한계를 다시 설명하는 내용이다. 앞선 내용과 중복되어 독자에게 혼란을 준다. → 라인 1559~1565의 단락(그러나 다음과 같은 요구사항을 가진 stateful 워크로드에는 적합하지 않다...)을 삭제한다. 이 내용은 이미 라인 1527~1531의 Deployment 한계 목록에서 다루었다.

### daily/day01.md
- **HIGH** `실습재현불가` _문제 2~5 풀이 과정 <details> 내부 — 스크린샷 없음_ — 문제 2(static-httpd 생성), 문제 3(join-command.txt), 문제 4(apiserver-config.txt), 문제 5(prod-server.txt) 풀이 결과에 검증 스크린샷이 없다. 특히 문제 3·4의 코드 주석 '# 출력 예:', '# 출력:' 다음 행에 텍스트 출력이 코드 주석으로 삽입돼 있어 CLAUDE.md 4-1절 규칙(출력은 이미지로)을 위반한다. → 문제 2~5 각각의 '검증 - 기대 출력:' 라인 뒤에 실제 kubectl/cat 실행 결과 스크린샷(images/day01-11-*.png ~ day01-14-*.png)을 삽입한다. 코드 주석 '# 출력 예:'·'# 출력:' 행은 스크린샷으로 대체하거나 제거한다.
- **HIGH** `구조` _섹션 4(실전 시험 문제)와 섹션 5(tart-infra 실습) 사이_ — CLAUDE.md 5절 일별 양식에 명시된 '직접 해보기(시험형 미니랩, 시간 제약 의식)' 절이 없다. 5문제는 이미 실전 문제 형식이지만 시간 제한(예: '5분 이내')을 의식한 독립 드릴 섹션이 구분되지 않아 '풀어보고 확인'하는 훈련 구조가 약하다. → 각 문제 또는 별도 '직접 해보기' 절에 예상 소요 시간(예: 문제 1 목표: 3분)을 명시하고, 학생이 스스로 시간을 재며 풀 수 있도록 힌트와 풀이를 완전히 분리한 구조를 추가한다.
- **MED** `용어비약` _1.5 Static Pod 심화, 매니페스트 디렉터리 트리 코드블록(425~432행)_ — 디렉터리 구조를 ASCII 트리 코드블록으로 표현했는데 CLAUDE.md 4-2절은 'ASCII 박스 다이어그램 금지'를 명시한다. 단, 4-2절은 '디렉터리 트리는 예외'라고 인정하므로 규칙 위반은 아니나, 이 트리가 실제 노드에서 ls 한 출력인지 예시인지 명확히 표기되지 않아 학생이 '실측 캡처인가, 예시인가'를 구분하기 어렵다. → 트리 블록 위에 '실측 캡처 아님 - 경로 구조 도식' 등의 캡션 한 줄을 추가하거나, staticPodPath 내용을 실제 ls -la 스크린샷으로 보충한다.
- **MED** `맥락점프` _1.6 kubeadm 완벽 이해 - kubeadm 핵심 명령어(507~540행)_ — kubeadm upgrade apply/node 명령이 등장하지만 업그레이드 절차(노드 drain, kubelet 업그레이드, uncordon 순서)가 전혀 설명되지 않는다. day04에서 다룬다 해도 여기서 학생이 명령을 보고 '어떤 순서로 써야 하는가'를 알 수 없다. → '업그레이드는 day04 참조'처럼 명시적 순방향 링크를 달거나, kubeadm upgrade 명령 블록에 '반드시 kubectl drain 후 실행' 등 최소 주의사항 한 줄을 추가한다.
- **MED** `스크린샷부족` _6.2 API 서버에 접속할 수 없는 경우 - crictl logs 명령(1083~1085행)_ — crictl logs ... 명령이 있지만 실제 에러 로그 스크린샷이 없다. crictl ps 결과는 day01-08로 캡처됐지만 logs 출력은 누락됐다. → 잘못된 kube-apiserver 매니페스트(예: etcd endpoint 오타)로 의도적으로 장애를 만든 후 crictl logs 출력을 캡처해 day01-xx-crictl-logs.png 로 삽입한다.
- **LOW** `이해난이도` _1.8 인증서 구조 완벽 정리(628~661행) - front-proxy 설명_ — front-proxy-ca.crt / front-proxy-client.crt 의 역할이 'API aggregation layer용'이라고만 표기돼 있다. aggregation layer 자체가 day01에서 처음 등장하는 개념인데 풀이 없이 나온다. → front-proxy 항목에 한 줄 직관('외부 API를 K8s API로 확장하는 레이어로, custom metrics 등이 이를 통한다')을 추가하고 day06 등 상세 내용과의 링크를 달아 맥락을 연결한다.
- **LOW** `스토리라인누락` _2.2 Watch 메커니즘(687~701행)_ — Watch 메커니즘의 등장 배경이 서술되지 않는다. 폴링(polling) 방식의 한계(CPU 낭비, 반응 지연)가 먼저 나온 뒤 Watch가 이를 어떻게 해결했는지의 비교가 없어 '왜 이게 나왔나'가 빠져 있다. → Watch 절 첫 문단에 '폴링 방식에서는 N초마다 전체를 조회해 CPU와 네트워크를 소모했다'는 직전 방식 설명을 1~2문장 추가하고 Watch가 이 문제를 해결하는 방식으로 연결한다.

### daily/day02.md
- **HIGH** `스크린샷부족` _문제 9(Pod CIDR), 문제 10(kube-system Pod 상태), 문제 12(kubeconfig 컨텍스트 생성), 문제 13(etcd data-dir), 문제 14(네임스페이스 비교), 문제 15(Control Plane 포트) 풀이 과정 내부_ — 6개 문제의 검증 출력이 '# 기대: ...' 형태의 텍스트 주석으로만 제시된다. CLAUDE.md 절대규칙 4.1은 '명령 실행 결과는 실제 터미널 스크린샷 이미지'를 요구하며 텍스트 출력 블록을 금지한다. → 각 문제의 검증 명령을 dev/platform 클러스터에서 실제 실행 후 스크린샷을 캡처해 images/day02-10 ~ day02-15 형태로 저장하고 '검증 - 기대 출력:' 아래에 ![...](images/...) 로 교체한다.
- **HIGH** `스크린샷부족` _tart-infra 실습 3: API Server 접근 과정 확인 (L.1035-1052)_ — kubectl get nodes -v=6 명령의 출력(HTTP 요청 로그)이 스크린샷 없이 동작 원리 텍스트 설명만 있다. 다른 실습(1/2/4)은 스크린샷이 있어 일관성이 깨진다. → 실제 터미널에서 kubectl get nodes -v=6 를 실행하고 HTTP GET 요청 라인이 보이는 화면을 캡처해 images/day02-10-apiserver-verbose.png 로 삽입한다.
- **HIGH** `구조` _H2 섹션 번호 — 문서 전체 구조 (L.24, L.405, L.821, L.922, L.879, L.894)_ — 구성 안내(L.20)는 '§1 -> §5 -> §6 -> §7' 순서라고 명시하는데 §2/§3/§4 가 통째로 없다. 또한 5.16(L.879)과 5.17(L.894) 절이 §6 트러블슈팅 내부에 삽입돼 있어 5.x 계열 예제가 §6 안에 섞인다. → §2(개념 요약 또는 Day1 복습)·§3·§4 가 의도적으로 생략된 것이라면 구성 안내에서 그 번호를 삭제한다. 5.16/5.17 절은 §6 앞(§5 마지막)으로 이동해 번호 연속성을 맞춘다.
- **HIGH** `실습재현불가` _5.11 ConfigMap 환경변수 Pod (L.691-711), 5.12 Secret Pod (L.717-743)_ — 두 YAML 예제가 참조하는 app-config ConfigMap 과 db-secret Secret 이 문서 어디에도 생성 명령 없이 쓰인다. 학생이 kubectl apply 를 시도하면 'configmap not found' 오류로 실패한다. → 각 예제 앞에 kubectl create configmap app-config --from-literal=special.key=hello 와 kubectl create secret generic db-secret --from-literal=password=pass 명령을 선행 단계로 추가한다.
- **MED** `스토리라인누락` _[등장 배경] 블록 전체 — CLAUDE.md §4.4 4요소 기준_ — CLAUDE.md 가 요구하는 스토리라인 4요소 중 '직전 기술과의 차이(이전 방식·한계)'가 대부분 누락됐다. 예: 레이블(직전: 노드 수동 관리), Taint(직전: 없을 때 Control Plane에 일반 Pod 배치), kubeconfig(직전: 매 명령마다 --server/--certificate 수동 입력) 등 '이전에는 어떻게 했는가'가 없어 스토리가 반쪽이다. → 각 [등장 배경] 블록에 '이 기능이 없던 시절 운영자는 어떻게 했는가, 그 방식의 구체적 불편·한계'를 1-2문장 추가한다.
- **MED** `맥락점프` _문서 도입부 (L.1-22) — 시험 속도 설정 안내_ — CLAUDE.md §4.3은 시험 첫머리에 alias k=kubectl / export do='--dry-run=client -o yaml' 설정 안내를 지시하는데 day02에 없다. 이 설정 없이는 §7 핵심 명령어 암기의 --dry-run=client -o yaml 패턴을 실습할 때 타이핑이 느려진다. → 오늘의 학습 목표 바로 아래에 '시험 환경 초기 설정' 블록(alias k=kubectl, export do=..., KUBE_EDITOR=nano)을 추가한다.
- **MED** `이해난이도` _문제 15 풀이 (L.363-400) — etcd 포트_ — 학습 목표(L.17)에 '포트 4개(6443/10259/10257/2379)를 암기하고 확인'이라고 했으나 etcd peer 포트 2380이 설명·명령 어디에도 없다. kubectl grep 이 listen-client-urls 만 잡아 2380이 빠진다. 실제 시험에서 2380 peer 통신 여부를 묻는 문제가 출제된다. → 문제 15 [등장 배경] 또는 풀이 핵심 칸에 etcd 포트 2379(클라이언트)·2380(peer 간 raft 통신) 구분을 추가하고, ssh platform-master 후 ss 명령에서 2380도 확인하는 줄을 넣는다.
- **MED** `용어비약` _5.13 ServiceAccount (L.745-763)_ — RBAC 라는 단어가 '거기에 RBAC로 권한을 붙인다'로만 언급되고 첫 등장 풀이가 없다. Role-Based Access Control 약자와 '어떤 오브젝트·동사를 허용하는 규칙'이라는 한 줄 설명이 없으면 학부 3학년은 RBAC가 무엇인지 모른 채 넘어간다. → RBAC 첫 언급(L.747) 옆에 '(RBAC: Role-Based Access Control — 어떤 사용자/SA가 어떤 리소스에 어떤 동작을 할 수 있는지 정의하는 Kubernetes 권한 시스템, Day N에서 상세히 다룬다)' 한 줄 풀이를 추가한다.
- **LOW** `스크린샷부족` _문제 11 풀이 과정 (L.215-235) — kubeadm certs 실행 스크린샷_ — 문제 11 풀이 내부에는 스크린샷이 없다. 6.2 트러블슈팅 절(L.853)에 동일 명령의 스크린샷(day02-05)이 있으나, 문제 풀이 섹션에서 직접 연결하는 '검증' 블록이 없어 학생이 풀이를 보고 검증 화면으로 이동하는 흐름이 끊긴다. → 문제 11 풀이 </details> 직전에 '검증: 6.2절 day02-05 스크린샷 참고' 링크 또는 동일 이미지 재참조를 한 줄 추가한다.
- **LOW** `구조` _§7 복습 체크리스트 (L.922-965) — 자가점검 부재_ — CLAUDE.md 일별 양식은 '자가점검(<details> 정답)' 절을 요구하는데, day02 §7은 체크박스 목록만 있고 정답을 숨긴 <details> 자가점검이 없다. 학생이 개념을 스스로 확인할 수단이 없다. → §7 아래에 5-7개 단답형 문제(예: 'etcd 기본 --data-dir 경로는?', 'Taint effect 3종을 말하라')를 <details><summary>정답 보기</summary> 형태로 추가한다.

### daily/day03.md
- **MED** `실습재현불가` _4.3절 Control Plane 업그레이드 Step 1, 4.4절 Worker 업그레이드 Step 2_ — apt-get install -y kubeadm=1.31.0-1.1 전에 쿠버네티스 apt 저장소를 올바른 마이너 버전 채널(예: pkgs.k8s.io/core:/stable:/v1.31/deb)로 전환하는 단계가 없다. tart 클러스터는 구버전(v1.31) 저장소가 이미 설정되어 있을 수 있지만, 저장소 변경 없이 apt-get update를 해도 대상 버전이 목록에 나타나지 않을 수 있어 학생이 재현에 막힌다. → 4.3 Step 1 앞에 저장소 전환 명령(echo deb [signed-by=...] https://pkgs.k8s.io/core:/stable:/v1.31/deb/ / | sudo tee /etc/apt/sources.list.d/kubernetes.list)을 추가하고, 이 저장소가 마이너 버전별로 분리된 이유(이전 버전 패키지를 채널에서 제거해 실수로 건너뛰기를 막음)를 한 줄 설명한다.
- **MED** `스크린샷부족` _4.3절 kubeadm upgrade plan 출력(365-379행), tart-infra 실습 업그레이드 실습 없음_ — kubeadm upgrade plan 실행 결과가 코드블록 주석(# 출력 예:)으로만 제시되어 있고 실측 스크린샷이 없다. 또한 4.3/4.4절의 핵심 명령(upgrade apply, upgrade node, kubectl get nodes 버전 확인)은 tart-infra 실습 섹션에 대응 실습이 없어 학생이 실제 출력을 눈으로 확인할 수 없다. → staging 클러스터에서 kubeadm upgrade plan 실행 결과 스크린샷(day03-12-upgrade-plan.png)을 추가하고, tart-infra 실습에 실습 5(staging 클러스터 kubeadm upgrade plan 실행 후 출력 확인)를 간략히 추가한다. 전체 upgrade apply 실습은 클러스터 파괴 위험이 있으므로 dry-run 수준으로 제한해도 무방하다.
- **LOW** `용어비약` _1.3절 etcdctl 명령 첫 사용 전, 2.2절 snapshot save 명령_ — etcdctl 바이너리가 어디서 오는지 전혀 언급되지 않는다. kubeadm은 etcdctl을 자동 설치하지 않으며, 시험 환경에서는 /usr/local/bin/etcdctl 등에 미리 설치되어 있지만 학생이 로컬에서 재현할 때 command not found로 막힌다. → 1.3절 앞에 한 줄 삽입: etcdctl은 etcd와 별도 제공되는 CLI 도구로 시험 환경에는 /usr/local/bin 에 사전 설치된다. 로컬 재현 시 ETCD_VER=v3.5.x를 지정해 공식 릴리스 페이지에서 직접 내려받는다. (설치 명령 2줄 예시 추가)
- **LOW** `실습재현불가` _tart-infra 실습 4 5단계(1112-1115행)_ — etcd restore 후 kubectl get nodes, kubectl get pods -A 결과를 확인하는 5단계에 스크린샷이 없다. 클러스터 정상 복구 여부를 시각적으로 증명하는 단계임에도 이미지가 빠져 있어 학생이 '내 결과가 맞는지' 비교할 기준이 없다. → 실습 4 5단계 직후에 kubectl get nodes와 kubectl get pods -A 결과 스크린샷(day03-13-restore-verify.png)을 추가한다.
- **LOW** `맥락점프` _tart-infra 실습 2, 1021행 kubectl get pods -n demo -o wide_ — 실습 2에서 dev 클러스터의 demo 네임스페이스에 파드가 있다고 가정하고 조회 명령을 실행하지만, demo 네임스페이스가 언제 어떻게 생성되었는지 설명이 없다. 클러스터에 아무것도 없는 상태라면 결과가 비어 있어 실습 목적(Pod 분포 확인)이 달성되지 않는다. → 실습 2 앞에 demo 네임스페이스와 테스트 Pod 생성 명령(kubectl create ns demo && kubectl run demo-app --image=nginx -n demo) 1~2줄을 추가하거나, -n demo 대신 -A를 사용해 네임스페이스 사전 조건을 제거한다.

### daily/day04.md
- **HIGH** `실습재현불가` _문제 7 풀이 과정 (line 171~204)_ — Worker Node 업그레이드 스크립트에 apt 저장소 전환 단계(sudo sed -i 's/v1.30/v1.31/' /etc/apt/sources.list.d/kubernetes.list)가 빠져 있다. 문제 6에서 'pkgs.k8s.io는 마이너 버전별 저장소가 분리돼 저장소를 먼저 바꾸지 않으면 즉시 실패한다'고 명시했음에도 worker 스크립트에는 sudo apt-get update 직전에 이 단계가 없어, 그대로 따라 하면 apt-get install이 실패한다. → 문제 7 풀이의 '# 2. SSH 접속' 바로 다음에 Control Plane 업그레이드와 동일하게 저장소 전환 명령(sudo sed -i 's/v1.30/v1.31/' /etc/apt/sources.list.d/kubernetes.list)과 실패 이유 주석을 추가한다.
- **HIGH** `스크린샷부족` _문제 7, 8, 9, 10, 11, 12 각 풀이 과정 <details> 내부_ — 문제 7~12 풀이 섹션에는 '검증 - 기대 출력' 항목과 ![](images/..) 스크린샷이 전혀 없다. 문제 5, 6에는 day04-01, 02, 03 이미지가 있고 tart-infra 실습 섹션에는 day04-08~12 이미지가 있지만, 정작 풀이 본문에서 학생이 가장 먼저 보는 문제 7~12 풀이 블록은 텍스트 명령만 나열된다. CLAUDE.md 절대규칙(4-①)에 위반된다. → 문제 7 kubectl get nodes 최종 확인, 문제 8 etcdctl member list 출력, 문제 12 drain 실패 오류 메시지 및 해결 후 get nodes 결과에 각각 실측 스크린샷을 삽입한다. 파괴적 실습(업그레이드)은 '(미캡처)'로 명시한다.
- **MED** `맥락점프` _## 8. 추가 YAML 예제, ## 9. 복습 체크리스트 섹션 번호 (line 402, 533)_ — 문서 H2 섹션이 '## 오늘의 학습 목표', '## 실습 환경 설정'으로 시작한 뒤 문제 5~12는 H3(###)으로 처리되다가 갑자기 '## 8. 추가 YAML 예제', '## 9. 복습 체크리스트'로 번호가 붙는다. 1~7번 H2 섹션이 존재하지 않아 번호 체계가 불연속적이고, 학생 입장에서 '8번 이전에 무엇이 있었나'하는 혼란을 준다. → 번호를 제거해 '## 추가 YAML 예제', '## 복습 체크리스트'로 변경하거나, 문제 5~12를 H2로 올리고 번호를 재정렬한다.
- **MED** `맥락점프` _tart-infra 실습 섹션 (line 562~660)_ — tart-infra 실습 섹션이 문서 후반부에 독립 배치되어 문제 5~12 풀이와 분리된다. 학생이 문제 풀이를 읽을 때 '검증 결과는 어떻게 나오나'를 즉시 확인할 수 없고, 한참 뒤 tart-infra 섹션에서야 platform 클러스터 스크린샷을 보게 된다. 특히 etcd health(day04-11)와 cordon(day04-10)은 문제 8, 5에 대응하지만 섹션이 멀리 떨어져 있어 연결이 끊긴다. → tart-infra 실습의 platform 실측 캡처를 각 해당 문제(문제 8 -> etcd health, 문제 5 -> cordon) 풀이 검증 블록 바로 아래에 이동하거나 상호 참조 링크를 추가한다.
- **MED** `용어비약` _문제 11 풀이 과정 (line 321~336)_ — 문제에서 '만료까지 30일 이내인 인증서가 있는지 확인하라'고 지시하지만 풀이 스크립트에는 30일 기준을 자동으로 걸러내는 명령이 없다. kubeadm certs check-expiration 전체를 출력하고 openssl 루프를 돌리는 것으로 끝나며, 30일 이내인지 판단하는 방법(EXPIRES 컬럼 읽는 법, openssl -checkend 2592000 등)이 없어 학생이 실제로 어떻게 '30일 이내'를 판별하는지 알 수 없다. → kubeadm certs check-expiration 출력의 EXPIRES 컬럼 읽는 법을 설명하고, openssl x509 -checkend 2592000 -noout 명령(30일=2592000초)을 추가해 30일 이내 인증서를 필터링하는 스크립트를 제공한다.
- **MED** `스크린샷부족` _문제 6 풀이 검증 블록 (line 152~155)_ — day04-03-versions.png 이미지 바로 아래에 '# (예시 -- 환경에 따라 다름. 파괴적 업그레이드라 실측하지 않음)'이라는 주석이 코드블록 안에 인라인으로 들어 있다. 이미지 자체는 존재하지만 캡션과 실측 여부 표시가 마크다운 이미지 설명이 아닌 코드 주석으로 숨겨져 있어, 렌더링 시 이미지 아래에 미캡처 설명이 보이지 않는다. → 코드 주석을 이미지 아래 본문 텍스트 '(파괴적 업그레이드라 실측하지 않음. 참고용 캡처)'로 옮긴다.
- **LOW** `이해난이도` _문제 9 풀이 과정 drain 시뮬레이션 (line 276)_ — kubectl drain --dry-run=client 를 사용하지만, kubectl drain 은 v1.25+ 에서 --dry-run 대신 실제로는 내부적으로 eviction API 를 쓰기 때문에 --dry-run=client 가 Pod 퇴거를 완전히 시뮬레이션하지 않는다는 주의 사항이 없다. 학생이 dry-run 결과가 실제 drain 과 동일하다고 오해할 수 있다. → '--dry-run=client 는 대상 Pod 목록을 나열할 뿐 실제 eviction API 호출 없이 클라이언트에서만 평가한다. PDB 위반 여부는 dry-run 으로 확인되지 않는다' 는 주의 한 줄을 추가한다.
- **LOW** `구조` _학습 목표 체크리스트 vs 복습 체크리스트 (line 9~14, 533~542)_ — 오늘의 학습 목표 4항목(cordon 메커니즘, CP 업그레이드 7단계, etcdctl 4옵션, drain 실패 4원인)과 복습 체크리스트 6항목이 부분적으로 겹치지만 완전히 일치하지 않는다. 목표에는 있는 'etcdctl member list 4옵션 암기'가 복습 체크리스트에서는 snapshot save 4옵션으로 대체된다(Day 3 복습이 Day 4 체크리스트에 섞임). 학생이 Day 4 학습 목표와 최종 점검이 어긋난다고 느낀다. → 복습 체크리스트를 Day 4 학습 목표(cordon 메커니즘, CP 업그레이드 순서, etcdctl member list 옵션, drain 실패 원인 4가지)와 1:1 대응하도록 정렬하고, Day 3 내용(snapshot save 옵션)은 '이전 내용 재확인' 항목으로 분리한다.

### daily/day05.md
- **HIGH** `스크린샷부족` _문제 2(sarah-viewer), 문제 3(cicd-bot), 문제 4(CSR newadmin), 문제 5(get-contexts), 문제 6(full-developer) 풀이 <details> 블록_ — 문제 2~6 풀이 결과에 검증 스크린샷이 전혀 없다. 문제 1만 day05-01-tom-cani.png를 첨부했고 나머지 5개 문제는 명령만 있고 출력 확인 이미지가 없다. 학생이 자신의 실행 결과가 맞는지 비교할 기준이 없다. → 각 문제 풀이 끝에 `kubectl auth can-i`/`kubectl config get-contexts`/`openssl x509 -subject` 결과를 실측 스크린샷으로 첨부한다. 최소 문제 4(CSR 발급 후 subject 확인)와 문제 5(get-contexts 목록)에는 반드시 추가한다.
- **HIGH** `스크린샷부족` _섹션 3.2 kubeconfig 생성 5단계 전체_ — kubeconfig 수동 생성 명령 5단계가 모두 bash 코드블록으로만 제시되고, 어떤 단계에도 실행 결과 스크린샷이 없다. `kubectl config get-contexts`나 `kubectl --kubeconfig=/tmp/my-user.kubeconfig get pods`의 출력이 어떻게 보이는지 학생이 확인할 방법이 없다. → Step 4(use-context) 이후 `kubectl config get-contexts` 결과 스크린샷과, Step 5 테스트 명령 출력 스크린샷을 추가한다.
- **MED** `스크린샷부족` _섹션 8.1 트러블슈팅, 969번째 줄 `kubectl describe role app-manager -n demo`_ — `kubectl describe role` 명령이 코드블록에 있으나 출력 스크린샷이 없다. 본문에서 PolicyRule.Resources·APIGroups 필드를 확인하라고 설명하지만 실제 출력 모양을 보여주지 않아, 처음 보는 학생은 어느 필드를 어디서 찾아야 하는지 알기 어렵다. → `kubectl describe role app-manager -n demo` 실측 스크린샷을 8.1 절에 추가한다.
- **MED** `실습재현불가` _섹션 3.2 kubeconfig 생성 Step 5(테스트)_ — `kubectl --kubeconfig=/tmp/my-user.kubeconfig get pods`를 실행하면 사용자 인증서가 아직 CA 서명을 받지 않은 상태이거나 RBAC 권한이 없어 Forbidden이 날 수 있는데, 이에 대한 사전 안내가 없다. 섹션 3은 섹션 4(CSR)보다 앞에 위치하여, 순서대로 따라 하면 4단계에서 생성할 인증서가 아직 없는 상태에서 Step 2에서 `--client-certificate=/tmp/my-user.crt`를 참조해 혼란을 준다. → 섹션 3.2 도입부에 '이 예시는 섹션 4에서 생성할 인증서 파일을 전제한다. 섹션 4를 먼저 실습한 뒤 돌아오거나, 파일 경로를 실제 생성한 파일로 교체한다'는 안내를 추가한다.
- **MED** `용어비약` _섹션 2.2 Pod에서 ServiceAccount 사용, `automountServiceAccountToken: true (기본: true)` 주석_ — automountServiceAccountToken 기본값이 true라고만 설명하는데, 보안 관점에서 API 서버 접근이 필요 없는 Pod는 false로 설정해야 한다는 최소 권한 원칙 설명이 없다. 이후 트러블슈팅 8.3에서 false 케이스가 나오지만 왜 false를 쓰는지 맥락이 먼저 없다. → 섹션 2.2에 'API 서버 접근이 불필요한 워크로드는 `automountServiceAccountToken: false`를 설정하는 것이 최소 권한 원칙에 맞다. SA 또는 Pod spec 어느 쪽에 설정해도 Pod 단위로 적용된다'는 1-2문장을 추가한다.
- **LOW** `맥락점프` _섹션 1.3 API Groups 바로 다음, 섹션 1.4 Verbs 목록_ — `deletecollection` verb가 표에만 나오고 언제 실제로 쓰이는지 예시가 없다. 학부생은 이것이 `kubectl delete pods --all`과 대응되는지 알기 어렵다. → deletecollection 행의 설명에 '`kubectl delete pods --all`처럼 컬렉션 전체를 한 번에 삭제할 때 사용된다'는 짧은 예시를 괄호 안에 추가한다.
- **LOW** `이해난이도` _섹션 4.2 CSR Step 3, `$(cat /tmp/newuser.csr | base64 | tr -d '
')` 인라인 치환_ — Heredoc 안에 `$(...)` 명령 치환이 포함된 패턴을 처음 보는 학생은 이것이 실행 시 평가되는지, 파일에 리터럴로 들어가는지 혼동할 수 있다. 특히 이 패턴이 실패하는 셸 환경(일부 zsh 설정)에 대한 주의가 없다. → Step 3 코드블록 아래에 '`$(...)` 치환이 실행 시 CSR 내용을 base64로 변환해 YAML에 삽입한다. 치환이 제대로 안 되면 `CSR_B64=$(cat /tmp/newuser.csr | base64 | tr -d "\n")`으로 변수에 먼저 저장한 뒤 `request: $CSR_B64`로 대입하는 2단계 방식을 사용한다'는 주석을 추가한다.
- **LOW** `구조` _섹션 7 실전 시험 문제 서두, 577번째 줄 '나머지 6문제는 day06에서'_ — 섹션 제목이 '6문제'인데 실제로 6문제가 있으므로 '나머지 6문제'라는 표현이 혼란을 준다. 'day05에서 6문제, day06에서 추가 6문제'라는 구조인지 'day05에 6문제가 전부이고 day06에 별도 유형 6문제'인지 불명확하다. → 577번째 줄 blockquote를 '이 절에서는 RBAC 핵심 6문제를 다룬다. kubeconfig 생성과 권한 확인 집중 연습 유형 6문제는 day06에서 이어진다'로 명확히 수정한다.

### daily/day06.md
- **HIGH** `구조` _문서 전체 섹션 번호 체계 — 섹션 0 다음이 바로 섹션 8(## 8. 추가 YAML 예제)로 점프한다. 섹션 1~7이 없다._ — 독자가 섹션 1~7을 찾다가 혼란에 빠진다. 문제 7~12가 섹션 번호 없이 ### 으로만 존재해, 섹션 8과의 위계 관계가 불분명하다. day05 에서 넘어오는 문제 번호(7~)도 문서 내에서 연결 설명이 없어 '문제 1~6은 어디에 있는가'를 알 수 없다. → 문제 7~12 묶음을 '## 1. CKA 실기 문제 7~12' 같은 섹션으로 감싸거나, 문서 첫머리에 '이 파일의 문제 7~12는 day05(문제 1~6)에 이은 연속 번호'임을 명시하고 day05.md 링크를 추가한다. 또는 섹션 번호를 0, 1, 2... 순서로 재부여해 독자가 목차를 따라갈 수 있게 한다.
- **HIGH** `실습재현불가` _문제 11(CSR 거부) 풀이 과정 — 'suspicious-user CSR 이 Pending 상태이다'라고만 가정하고, 실습자가 그 CSR 을 직접 만들어 Pending 상태로 두는 사전 설정 명령이 없다._ — 학생이 실제로 실습하려면 먼저 suspicious-user 의 CSR 객체를 생성해야 하는데, 그 단계(openssl genrsa -> openssl req -> kubectl apply CSR YAML)가 풀이 안에 없다. 시험에서는 CSR 이 이미 있지만 로컬 실습에서는 없으므로 재현 불가능하다. → 풀이 과정 앞에 '실습 사전 설정: 아래 명령으로 suspicious-user CSR 객체를 Pending 상태로 만든다' 블록을 추가한다(openssl genrsa, openssl req -subj '/CN=suspicious-user', base64 인코딩, kubectl apply CertificateSigningRequest). 시험 환경에서는 이 단계가 이미 완료돼 있다는 주석도 달면 된다.
- **HIGH** `실습재현불가` _문제 12(권한 감사) 풀이 과정 — 사용자 tom 의 RoleBinding 이 사전에 존재해야 의미 있는 출력이 나오는데, 그 설정이 어디에도 없다._ — kubectl auth can-i --list --as=tom -n demo 를 실행하면 tom 에게 RoleBinding 이 없는 경우 selfsubjectaccessreviews 등 기본 항목만 나와, 스크린샷(day06-08)에 보이는 'pods [get list watch]' 항목이 재현되지 않는다. 어떤 Role/RoleBinding 이 미리 설정돼 있어야 하는지 알 수 없다. → 풀이 앞에 '실습 사전 설정: tom 에게 pod-reader Role 을 바인딩한다(kubectl create role ... && kubectl create rolebinding ...)' 블록을 추가한다. 시험 환경에서는 이미 바인딩이 있다는 주석도 포함한다.
- **MED** `용어비약` _추가 심화 학습 > CertificateSigningRequest 처리 절차, 761행 signerName 필드_ — signerName: kubernetes.io/kube-apiserver-client 가 코드에 나오지만 이 값이 무엇인지, 다른 signerName(kubernetes.io/kubelet-serving 등)과 언제 구분해야 하는지 설명이 없다. CKA 시험에서 signerName 을 잘못 지정하면 CSR 이 서명되지 않아 실패한다. → signerName 필드 주석에 '클라이언트 인증서(kubectl 용)에는 kubernetes.io/kube-apiserver-client, kubelet 서빙 인증서에는 kubernetes.io/kubelet-serving 을 쓴다' 한 줄을 추가하고, CKA 시험 기준 이 두 값만 기억하면 된다는 안내를 넣는다.
- **MED** `맥락점프` _문제 8(RBAC 분석) 풀이, 125~129행_ — 풀이 코드블록 안에 cat /tmp/cluster-admin-rules.txt 뒤에 텍스트 출력을 주석(# PolicyRule: ...) 형식으로 넣어 두었다. CLAUDE.md 규칙(명령 출력은 텍스트 블록 금지, 스크린샷으로만)에 위배되며, 이 부분에는 스크린샷이 없다. → 주석 형태의 출력 텍스트를 삭제하고 'kubectl describe clusterrole cluster-admin 의 실제 출력은 위 스크린샷(day06-05-cluster-admin.png) 참조' 문장으로 대체하거나, 문제 8 전용 스크린샷을 별도로 삽입한다.
- **MED** `이해난이도` _섹션 9 복습 체크리스트 / 시험 팁, 447~464행_ — RBAC verb 목록(get, list, watch, create, update, patch, delete, deletecollection, exec, bind, escalate, impersonate)이 문서 어디에도 한눈에 정리되어 있지 않다. 시험에서 'Deployment 를 수정할 수 있는 Role' 문제에서 update 와 patch 의 차이를 모르면 틀린다. → 복습 체크리스트 또는 시험 팁 안에 'K8s 주요 verb 빠른 참조표(get/list/watch = 읽기, create/update/patch/delete = 쓰기, exec/portforward/log = 특수)' 소형 표를 추가한다.
- **MED** `스토리라인누락` _문서 전체 — Node authorizer / Webhook 인가 모드 언급 없음_ — RBAC 등장 배경에서 ABAC 한계는 다루지만, 실제 API 서버는 --authorization-mode=Node,RBAC 로 복수 인가 모드를 동시에 쓴다는 사실이 없다. CKA 시험에서 Node authorizer 가 왜 kubelet 에게 필요한지(CSR 승인, Node API 접근)를 묻는 문제가 출제된다. → 섹션 0 트레이드오프 또는 심화 학습 초입에 '실제 클러스터의 인가 체인: --authorization-mode=Node,RBAC — Node authorizer 는 kubelet 전용(node 리소스 접근), RBAC 는 나머지 전용' 한 단락을 추가한다.
- **LOW** `맥락점프` _섹션 8.4 Aggregated ClusterRole, 399행_ — (미실측) 표기는 있으나, aggregation 이 언제 반영되는지(수 초 지연 가능) 확인 명령(kubectl get clusterrole view -o yaml | grep -A3 monitoring)의 기대 출력 스크린샷이 없다. 독자가 '내 클러스터에서는 왜 반영이 안 됐나'를 알 수 없다. → platform 클러스터(prometheus-operator 설치됨)에서 실측 후 스크린샷을 삽입하거나, '미실측 — platform 클러스터에서 실습 필요. CRD 없는 dev 에서는 동작 불가' 안내를 더 눈에 띄게 블록인용 대신 Warning 박스 형식으로 올린다.
- **LOW** `구조` _문서 읽기 순서 안내, 591행 blockquote_ — 읽는 순서 안내(문제 7~12 -> 섹션 8 -> 체크리스트 -> tart 실습 -> 심화)가 문서 중반부(591행, 추가 심화 학습 직전)에 묻혀 있어 처음 파일을 연 학생은 보지 못하고 지나친다. → 이 읽기 순서 안내를 '## 오늘의 학습 목표' 바로 아래(섹션 0 이전)로 이동시켜 첫 화면에서 보이게 한다.

### daily/day07.md
- **HIGH** `스크린샷부족` _tart-infra 실습 / 실습 1 (line 971)_ — 실습 1의 'kubectl get deployments -n demo', 'get deployment ... jsonpath strategy', 'get rs --sort-by' 세 명령 출력이 모두 '(미캡처)'로 표시되어 스크린샷이 없다. 학생이 이 명령을 처음 실행했을 때 어떤 컬럼/포맷이 나오는지 확인할 수단이 없다. → dev 클러스터에서 세 명령을 실행하고 터미널 스크린샷(day07-00-lab1-strategy.png)을 캡처해 삽입. '(미캡처)' 표기를 제거하고 실측 이미지로 교체.
- **HIGH** `실습재현불가` _실전 시험 문제 / 문제 2, 3 (lines 630~696)_ — 문제 2·3은 'demo 네임스페이스의 nginx-web Deployment'가 이미 존재한다고 전제하지만, 이를 사전에 생성하는 설정 명령이 없다. 학생이 독립적으로 따라 하면 'Error from server (NotFound): deployments.apps "nginx-web" not found'로 실패한다. → 각 문제 풀이 앞에 'nginx-web Deployment 사전 생성' 명령(kubectl create deployment nginx-web --image=nginx:1.24 --replicas=3 -n demo)을 명시하거나, 문제 1 뒤에 공통 환경 설정 블록으로 묶어 제공.
- **MED** `스크린샷부족` _tart-infra 실습 / 실습 3 (line 1022~1024)_ — day07-02-httpbin-deploy.png는 '형태 예시'이며 캡션에 '실측은 nginx 이미지로 시연'이라고 돼 있고 blockquote로 '이미지/AGE만 환경에 따라 다르다'고 보충한다. 즉 실제 httpbin v1/v2 출력이 아닌 대체 스크린샷이다. 학생 입장에서 version 라벨로 두 Deployment를 구분하는 실제 출력 형태를 확인하기 어렵다. → dev 클러스터에서 'kubectl create deployment httpbin-v1 --image=nginx:1.24 ... -l version=v1' 형태로 두 버전을 직접 만들어 실측하거나, 그것이 불가하면 캡션을 '형태 예시(미실측)'로 명확히 표기하고 blockquote 대신 일반 문단으로 이유를 설명.
- **MED** `맥락점프` _섹션 3.1 출제 유형 9번 (line 533) 및 섹션 4_ — 출제 유형 9번으로 'Deployment와 Service 연결'이 명시됐지만 실전 문제는 8개뿐이며 Service 연결 문제가 없다. 학습 목표로 예고한 유형을 연습할 기회가 빠져 있다. → 문제 9로 'kubectl expose deployment web-app --port=80 --target-port=80 --name=web-svc -n demo' 유형 문제를 추가하거나, 출제 유형 목록에서 9번을 day08 이후에서 다룬다는 안내로 대체.
- **MED** `이해난이도` _섹션 2.1 rollout pause/resume (lines 388~394)_ — pause/resume 명령이 소개됐지만, '일시정지 상태에서 set image를 두 번 호출하면 재개 시 한 번의 롤아웃으로 통합된다'는 핵심 사용 이유가 설명 없이 주석 한 줄(여러 변경을 한 번에 적용할 때 유용)로만 처리됐다. 학부생이 실제로 왜 쓰는지 이해하기 어렵다. → pause -> set image x2 -> resume 시나리오 3행 예제 코드와 '두 번의 set image가 각각 새 ReplicaSet을 트리거하지 않고 resume 시 한 번에 적용된다'는 1~2문장 설명 추가.
- **LOW** `구조` _tart-infra 실습 전체 (lines 948~1031)_ — 각 실습에 목표 시간(예: '3분 이내')이 없다. CLAUDE.md §4③에서 '속도와 손 숙련이 핵심, 시간 제약을 의식한 풀이 우선'을 요구하지만 실습 섹션에는 시간 지표가 하나도 없다. → 각 실습 제목 옆에 '(목표: N분)' 또는 섹션 도입부에 '시험 실기 기준 N분 이내 완료를 목표로 한다'는 한 줄 추가.
- **LOW** `용어비약` _섹션 1.3 YAML 주석 / namespace 설명 (line 117)_ — 주석에서 '리눅스 커널의 namespace와는 무관 -- 이름만 같다'고 했는데, 학부생이 쿠버네티스 네임스페이스와 리눅스 네임스페이스를 혼동할 경우 이 한 줄로는 부족하다. 리눅스 네임스페이스(pid/net/mnt 등)를 언급한다면 컨테이너 격리와의 관계를 한 문장이라도 설명하거나 해당 주석 자체를 삭제해 혼란을 방지하는 편이 낫다. → 주석을 '쿠버네티스 네임스페이스는 클러스터 내 리소스를 논리적으로 분리하는 범위 단위이며, 컨테이너 격리에 쓰이는 Linux kernel namespace(pid/net/mnt)와 이름이 같으나 별개 개념이다'로 보완하거나, 해당 부연 주석을 제거.

### daily/day08.md
- **HIGH** `스크린샷부족` _문제 11~19 각 풀이 과정 <details> 내부_ — 문제 11(revisionHistoryLimit), 12(Probe), 13(minReadySeconds), 14(sidecar), 15(history), 16(maxSurge=0), 17(restart), 18(matchExpressions), 19(ImagePullBackOff 진단)에 검증 스크린샷이 전혀 없다. 총 9개 문제 중 kubectl describe/get/logs 출력이 텍스트 주석(# 주석)으로만 표현돼 있어 학생이 자신의 출력과 비교할 기준이 없다. → 각 문제 풀이 마지막에 **검증 - 기대 출력:** 헤더를 달고 실제 터미널 스크린샷 PNG(day08-10 ~ day08-18 등)를 ![](images/..) 형식으로 삽입한다. 최소한 문제 12(Probe describe 출력), 19(ImagePullBackOff 및 rollout undo 후 이미지 확인)는 반드시 캡처한다.
- **HIGH** `정확성오류` _문제 15 풀이 과정 438~450행_ — 동일한 annotation 키(kubernetes.io/change-cause)를 --overwrite 플래그 없이 반복 사용하면 실제 kubectl에서 'already annotated' 오류가 발생한다. 현재 코드는 --overwrite 없이 3번 annotate를 반복해 학생이 그대로 복사 실행 시 에러를 만난다. → 두 번째 annotate 부터 kubectl annotate ... kubernetes.io/change-cause=... --overwrite 를 추가한다. 또는 kubectl set image 에 --record 대안 설명(deprecated 주의)을 함께 넣는다.
- **HIGH** `맥락점프` _섹션 6 복습 체크리스트 848행 — 'Deployment Conditions (Available, Progressing)의 의미를 아는가?'_ — 체크리스트에 Deployment Conditions를 아는가 묻지만 본문(섹션 0, 5, 각 문제)에서 Available/Progressing Condition이 무엇인지 전혀 설명하지 않는다. 학생은 체크리스트를 보고 해당 개념을 찾을 수 없다. → 섹션 5.1 Reconciliation Loop 다이어그램 아래에 'Deployment Conditions' 소항목을 추가한다: Available(가용 Pod >= minReadySeconds 충족 Pod 수), Progressing(롤아웃 진행 중 또는 완료), 그리고 kubectl get deployment -o wide의 AVAILABLE 필드와의 연결을 2~3줄로 설명한다.
- **MED** `구조` _섹션 5.2(780~810행), 5.3(812~829행) - 언어 태그 없는 plain text 블록_ — CLAUDE.md §4②에서 개념/흐름 그림은 mermaid 흑백을 표준으로 정하고 ASCII 다이어그램을 금지했다. 5.2 Rolling Update 시퀀스와 5.3 Pause/Resume 원리가 언어 태그 없는 ``` 블록(ASCII 텍스트)으로 작성돼 있어 규칙에 어긋난다. → 5.2는 mermaid sequenceDiagram(또는 flowchart)으로 Old RS/New RS 상태 전이를 시점별로 표현하고, 5.3은 flowchart LR로 pause -> 변경 누적 -> resume -> 단일 RS 생성 흐름을 그린다. 두 블록 모두 흑백 init 지시자를 첫 줄에 붙인다.
- **MED** `스토리라인누락` _문제 15(415~462행) — 이미지 변경 이력 추적_ — 다른 문제들과 달리 문제 15에는 '왜 이 필드/명령이 필요한가'를 설명하는 도입 blockquote가 없다. change-cause annotation이 왜 생겼는지, --record 플래그가 deprecated된 이유, rollout history가 ReplicaSet과 어떻게 연결되는지 맥락이 없어 스토리가 끊긴다. → 문제 15 상단에 > **왜 change-cause 가 필요한가** blockquote를 추가한다: rollout history의 CHANGE-CAUSE 열이 기본적으로 비어 있는 이유, --record가 v1.22+ deprecated된 경위, kubectl annotate로 직접 기록하는 현재 권장 방식을 3~4줄로 설명한다.
- **MED** `구조` _파일 전체 — CLAUDE.md daily 양식 대비 누락 섹션_ — CLAUDE.md §5 일별 양식에는 '## 자가점검 (<details> 정답)'과 '## 트러블슈팅 (흔한 실패 + 원인 + 복구 명령)' 섹션이 명시돼 있으나 day08.md에 두 섹션 모두 없다. 문제 19가 ImagePullBackOff를 다루지만 독립 섹션이 아니라 문제 풀이 안에 묻혀 있다. → 섹션 6 복습 체크리스트 앞에 '## 트러블슈팅' 섹션을 추가해 (1) ImagePullBackOff 원인/rollout undo 복구, (2) Endpoints 비어있을 때 selector 불일치 확인, (3) revisionHistoryLimit=0 으로 rollout undo 불가 케이스를 짧게 표로 정리한다. 자가점검은 복습 체크리스트 항목을 <details> 안에 답과 함께 넣는 형태로 보강한다.
- **LOW** `이해난이도` _문제 9 도입 blockquote 72행 — 'scale 변경을 여러 번 쌓아 둔 뒤'_ — pause 상태에서 scale 변경은 실제로 즉시 적용된다(섹션 5.3 818행에 명시). 그러나 문제 9 도입부에서 '이미지 변경·스케일 변경을 여러 번 쌓아 둔 뒤'라고 묶어 서술해 스케일도 pause 중에 지연되는 것처럼 읽힌다. 두 섹션이 모순돼 보인다. → 문제 9 도입 blockquote를 '이미지·환경변수 같은 Pod Template 변경은 쌓아 두고 한 번에 적용한다. scale(replicas) 변경은 pause 중에도 즉시 적용된다.'로 수정해 섹션 5.3과 일치시킨다.
- **LOW** `용어비약` _문제 10 도입 blockquote 121행 — 'Cilium, eBPF 로 패킷을 커널에서 분배하는 CNI'_ — eBPF와 Cilium이 갑작스럽게 등장하고 별도 설명이 없다. Day 8이 Deployment 심화 실전 문제를 다루는 날인데 Service/kube-proxy 대체 CNI를 처음 만나는 학생은 이 맥락이 낯설다. Cilium은 이 저장소 클러스터 특성이지 K8s 일반은 아니다. → '(또는 이 저장소의 dev 클러스터처럼 kube-proxy를 대체하는 Cilium — eBPF 기반 CNI, 자세한 설명은 certification/cilium/ 참조)' 식으로 괄호 처리하고 심화 링크를 달아 주 흐름을 방해하지 않게 한다.

### daily/day09.md
- **HIGH** `구조` _파일 전체 말미 -- 자가점검/시험팁/더읽을거리 섹션_ — CLAUDE.md daily 양식에 필수로 규정된 '자가점검(<details> 정답)', '시험 팁', '더 읽을거리' 섹션이 모두 누락됐다. 시험 출제 패턴(7절)이 부분적으로 대체하지만, 문제-정답 쌍 형태의 자가점검과 단일 명령형 숙지 팁은 별도로 필요하다. → ## 자가점검 섹션을 <details><summary>Q: ...</summary>A: ...</details> 형식으로 5~7문제 추가. ## 시험 팁 섹션에 alias k=kubectl, export do='--dry-run=client -o yaml' 셋업과 각 리소스 빠른 생성 명령 1줄 요약 추가. ## 더 읽을거리에 공식 문서 링크 추가.
- **HIGH** `스크린샷부족` _tart-infra 실습 섹션 -- 실습 2(HPA), 실습 3(PDB)_ — 두 실습의 이미지 앞에 '예상 출력 (예시 -- fresh dev 클러스터에는 ... 없어 No resources found 가 출력됨. 아래는 HPA가 설정된 환경의 형태)' 라는 면책 문구가 붙어 있어, 실제 실행 후 캡처가 아님을 스스로 인정하는 상태다. 이는 CLAUDE.md 4번 절대규칙('실제 터미널 스크린샷')을 위반한다. → 사전 준비 절차를 실제로 실행해(metrics-server 설치 -> HPA 생성 -> 부하 인가) 진짜 출력을 캡처한 스크린샷으로 교체. 또는 클러스터가 꺼진 경우 CLAUDE.md 규정대로 '(미캡처)'로 표기하고 이미지 참조 자체를 제거.
- **MED** `맥락점프` _tart-infra 실습 섹션 -- 실습 2(HPA), 실습 3(PDB)_ — day09 제목은 '스케줄링 심화 - Taint/Toleration, Affinity, DaemonSet'인데 tart-infra 실습 2~3이 HPA와 PDB를 다룬다. 왜 이 주제가 day09 실습에 포함됐는지 전환 설명이 없어 독자가 맥락을 잃는다. → 실습 2 도입부에 'HPA는 스케줄러가 Pod 수를 동적으로 조정하는 오토스케일링 레이어로, 이번 day09의 스케줄링 심화와 연결된다' 같은 한 문장 연결 설명을 추가. 또는 HPA/PDB 실습을 해당 주제 day(CKAD 등)로 이동 검토.
- **MED** `스토리라인누락` _섹션 4.3 CronJob이란?_ — Job(4.1)에는 등장배경+트레이드오프 서사가 상세히 작성됐으나 CronJob(4.3)에는 등장배경 단락이 없다. 'OS cron과 비교해 무엇이 다른가', '직전 해결책(시스템 crontab)의 한계', '트레이드오프(스케줄 미스 감지, missed 실행 누적)' 서술이 빠졌다. → 4.3 첫 단락에 등장배경 추가: 기존 시스템 crontab은 단일 호스트에 종속되어 노드 장애 시 실행이 보장되지 않고, 실행 이력을 K8s 레이어에서 관리할 수 없었다. CronJob은 이를 해결하며 successfulJobsHistoryLimit으로 이력 보존, startingDeadlineSeconds로 놓친 실행 정책을 선언적으로 관리한다. 트레이드오프: startingDeadlineSeconds 100초 미만으로 설정하면 컨트롤러 재시작 시 100회 이상 missed로 처리되어 CronJob이 비활성화될 수 있다.
- **MED** `실습재현불가` _섹션 6.1 Static Pod -- 명령어 블록 (line 902~909)_ — Static Pod 섹션에 'cat /var/lib/kubelet/config.yaml | grep staticPodPath' 명령과 'kubectl get pods -n kube-system' 명령이 있으나 실제 출력 스크린샷이 없다. Control Plane Static Pod 이름에 '-<nodeName>' 접미사가 붙는다는 설명도 명령 주석 한 줄뿐이고 실제 확인 화면이 없어 학생이 자신의 실습 결과와 비교할 수 없다. → ssh dev-master 후 kubelet config 조회 및 kube-system pods 목록을 실제 캡처한 스크린샷 추가. 또는 (미캡처) 표기.
- **LOW** `스토리라인누락` _섹션 2.5 nodeName으로 직접 배치_ — nodeName 사용법만 YAML로 제시하고 등장 배경, 스케줄러를 우회할 때의 위험성(리소스 초과 배치, 노드 장애 시 재스케줄 불가), 사용 시나리오 설명이 없다. → nodeName은 스케줄러를 완전히 우회하므로 노드의 실제 가용 리소스를 확인하지 않고 강제 배치한다. 노드가 이미 리소스를 초과했어도 Pod가 배치되며, 노드 장애 시 자동으로 다른 노드로 이동하지 않는다. 시험에서는 Static Pod 생성이나 특정 노드 고정 테스트에 드물게 등장한다는 용도 설명을 2~3줄 추가.
- **LOW** `스크린샷부족` _섹션 5.3 ResourceQuota -- 확인 명령어 블록 (line 866~869)_ — kubectl describe resourcequota 명령이 'Used vs Hard 표시'라고 설명하지만 실제 출력 스크린샷이 없다. ResourceQuota의 Used/Hard 열 구조를 보지 못한 학생은 출력을 해석하는 방법을 모른다. → demo 네임스페이스에 ResourceQuota를 실제 apply한 뒤 kubectl describe resourcequota 출력을 스크린샷으로 삽입.

### daily/day10.md
- **HIGH** `스크린샷부족` _문제 5,7,8,9,10,11,12,13,14,16,17,18,19,20 (각 <details> 풀이 내 검증 블록)_ — 17개 시험 문제 중 14개(문제 5,7,8,9,10,11,12,13,14,16,17,18,19,20)에 검증용 스크린샷이 없다. CLAUDE.md §4① 규칙상 명령 출력은 반드시 실제 터미널 스크린샷이어야 하며, 텍스트 출력 블록 대체도 금지돼 있다. 현재 이 문제들은 kubectl get 결과를 이미지로 보여주지 않아 학생이 자신의 실습 결과가 올바른지 육안으로 비교할 수 없다. → 각 문제 풀이의 검증 명령(kubectl get pod -o wide, kubectl describe 등) 직후에 실제 터미널 캡처 PNG 를 images/day10-XX.png 로 추가하고 ![](images/day10-XX.png) 로 삽입한다. 최소한 문제 9(NoExecute 축출 전후), 문제 13(Exists toleration, flex-pod Running on tainted node), 문제 17(multi-taint-pod Running on prod-worker1), 문제 20(graceful-pod Terminating 60s 후) 4개는 동작 증거로 필수다.
- **HIGH** `정확성오류` _문제 12 activeDeadlineSeconds Job, 풀이 내 command 설정_ — 문제는 'activeDeadlineSeconds 가 발동하는 상황'을 학습 목표로 하면서 명령을 sleep 30, deadline 을 60 으로 설정했다. sleep 30 은 deadline 60 보다 짧아 Job 이 정상 완료(Complete)되므로 deadline 이 작동하는 것을 전혀 관찰할 수 없다. 학생이 실행하면 Job 이 그냥 성공하고 끝나 activeDeadlineSeconds 의 동작을 체험하지 못한다. → command 를 sleep 120 (또는 sleep 300) 으로 변경해 deadline 60 이 실제로 작동해 Job 이 DeadlineExceeded 로 실패하는 것을 관찰하게 한다. 실패 확인 명령(kubectl describe job timeout-job -n demo | grep -A5 Conditions) 과 해당 스크린샷을 추가한다.
- **HIGH** `스크린샷부족` _tart-infra 실습 섹션, 실습 3: Resource Request/Limit 확인_ — 실습 3 의 kubectl get pods ... -o custom-columns 명령에 스크린샷이 없다. 다른 실습(1,2,4,트러블슈팅)은 스크린샷이 있는데 실습 3 만 누락돼 있어 일관성이 깨진다. → 실제 dev 클러스터에서 명령 실행 후 터미널 캡처 PNG(day10-10-resources.png 등) 를 추가한다.
- **MED** `용어비약` _문제 6 등장 배경 1행, 문제 18 등장 배경 1행_ — 'admission 단계에서 검증'이라는 표현이 두 번 사용되지만 admission 이 무엇인지(API Server 의 Admission Controller 플러그인 체인) 설명이 없다. 학부생은 이 용어를 처음 접한다. → 첫 등장(문제 6) 에서 'admission 단계(API Server 가 오브젝트 생성 요청을 etcd 에 저장하기 전 플러그인 체인이 검증·변형하는 단계)' 로 한 줄 풀이를 괄호 안에 추가한다.
- **MED** `구조` _복습 체크리스트 자가점검 Q&A 섹션_ — 체크리스트 항목은 13개(day11 예고 2개 포함 11개 실질 항목)인데 Q&A 는 8개뿐이다. activeDeadlineSeconds/ttlSecondsAfterFinished 의 차이, Pod Affinity 와 Anti-Affinity 의 차이, DaemonSet updateStrategy 두 옵션 비교, Job restartPolicy(Never vs OnFailure) 설명이 Q&A 에 없어 자가점검에서 빠진 학습 목표가 생긴다. → Q9(activeDeadlineSeconds 와 ttlSecondsAfterFinished 의 차이), Q10(Pod Affinity 와 Anti-Affinity 의 차이), Q11(DaemonSet RollingUpdate vs OnDelete), Q12(Job restartPolicy Never vs OnFailure) 를 추가한다.
- **MED** `스크린샷부족` _tart-infra 실습 4, HPA 예상 출력 블록_ — HPA 스크린샷(day10-07-hpa.png) 직전에 'fresh dev 클러스터에는 HPA 와 metrics-server 가 없어 No resources found 가 출력된다. 아래는 HPA+metrics-server 가 설정된 환경의 형태' 라고 명시돼 있다. 즉 학생이 실제로 따라 하면 보는 화면(No resources found)과 이미지가 달라 혼란을 준다. 실질적으로 '참조 예시' 성격이어서 CLAUDE.md §4① 에서 요구하는 '실측 출력' 이 아니다. → HPA 가 없는 경우의 실제 출력(No resources found)을 스크린샷으로 캡처해 메인 이미지로 쓰고, 현재 이미지는 '참고: HPA 설치 시 형태' 로 부제를 달아 별도 표시한다. 또는 HPA 생성 실습을 실습 4 에 포함해 실측 결과를 직접 캡처한다.
- **MED** `맥락점프` _파일 도입부 복습과 연결 섹션_ — 문제 7,12,14,16,18,19 가 demo 네임스페이스를 전제하지만 day10 에는 이 네임스페이스를 생성하는 명령이 없다. demo 네임스페이스는 day09 에서 생성됐으나 day10 도입부에 이 전제가 명시돼 있지 않아, day10 만 독립적으로 학습하는 학생은 demo 네임스페이스 없음 오류를 만난다. → 도입부 '복습과 연결' 또는 tart-infra 실습 환경 설정 섹션에 'demo 네임스페이스는 day09 에서 생성됐다. 없으면 kubectl create namespace demo 로 생성한다' 를 1줄 추가한다.
- **LOW** `스토리라인누락` _파일 전체, 개념 간 관계 설명_ — Taint/Toleration-Affinity-nodeSelector 세 메커니즘의 상호작용(예: nodeSelector 와 Affinity 를 동시에 쓰면 AND 조건, Taint 는 Affinity 보다 우선 적용됨)을 설명하는 다이어그램이나 요약표가 없다. CLAUDE.md §4② 는 개념-아키텍처-관계 그림에 mermaid 다이어그램을 필수로 요구한다. → 스케줄링 의사결정 흐름을 mermaid flowchart 로 작성한다(Taint 거부 -> nodeSelector 필터 -> Affinity 점수 -> 노드 선택). 흑백 themeVariables 지시자를 포함해야 한다.
- **LOW** `이해난이도` _문제 5 Node Affinity, requiredDuringSchedulingIgnoredDuringExecution 필드명 설명_ — 'IgnoredDuringExecution' 의 의미(실행 중 라벨 변경 시 기존 Pod 를 쫓아내지 않음) 는 설명돼 있으나, 향후 계획된 'RequiredDuringExecution' 이 아직 K8s 에 구현되지 않아 현재 유일한 옵션이라는 맥락이 없다. 학생이 '왜 Ignored 가 유일 선택지인가' 를 의아해 할 수 있다. → '현재 K8s 는 IgnoredDuringExecution 만 지원하며, RequiredDuringExecution(실행 중 라벨 변경 시에도 추방)은 아직 alpha/미구현 상태이다' 를 1줄 추가한다.

### daily/day11.md
- **MED** `실습재현불가` _tart-infra 실습 / 실습 2, 3 (line 1210-1251)_ — 실습 2(CoreDNS 조회)와 실습 3(Selector/EndpointSlice 추적) 명령이 demo 네임스페이스의 nginx/postgresql 서비스가 존재하는 전제로 작성돼 있으나, 앞선 실습 1(line 1162-1208)에서 cap-cka-d11 ns 를 만들었을 뿐 demo ns 를 만들거나 그 안에 서비스를 배포하는 셋업 명령이 없다. 학생이 그대로 따라 하면 'service not found' 오류가 발생한다. → 실습 2/3 앞에 demo ns 생성 및 nginx Deployment+Service 배포 셋업 명령을 추가하거나, 실습 2/3의 명령을 cap-cka-d11 ns 기준으로 통일한다.
- **MED** `스크린샷부족` _실습 2 (CoreDNS, line 1225), 실습 3 (line 1234), EndpointSlice (line 391), 미니랩 1/2/3 (line 1258-1316)_ — 실습 2에 스크린샷(day11-05-dns.png)은 있으나 실습 3 출력, EndpointSlice 조회, 미니랩 3종 모두 실제 터미널 캡처가 없다. 특히 3.2 EndpointSlice 절은 '(미캡처 - 클러스터 기동 후 실측 예정)' 문구만 남아 있어 규칙(CLAUDE.md §4①)을 위반한다. Headless Service DNS 검증도 스크린샷이 없다. → EndpointSlice 조회, 실습 3(endpointslice -n cap-cka-d11), 미니랩 1(get svc/endpoints 결과) 3개를 우선 캡처해 삽입한다. Headless nslookup 결과도 캡처가 필요하다.
- **MED** `실습재현불가` _실습 1 셋업 명령 (line 1179)_ — NodePort Service를 `kubectl expose --overrides` 방식으로 생성하는 명령이 포함돼 있으나, `--overrides`는 `kubectl run`에만 지원되고 `kubectl expose`에는 지원되지 않는다. 실제로 실행하면 unknown flag 오류가 발생한다. → `kubectl expose deployment backend --port=80 --target-port=80 --name=nginx --type=NodePort -n cap-cka-d11` 로 NodePort 만든 후, `kubectl patch svc nginx -n cap-cka-d11 -p '{"spec":{"ports":[{"port":80,"targetPort":80,"nodePort":30080,"protocol":"TCP"}]}}'` 로 nodePort 를 별도 지정하거나, YAML dry-run 방식으로 수정한다.
- **LOW** `용어비약` _4.1 CoreDNS란? (line 419)_ — SRV 레코드가 '포트 정보'라는 한 마디만으로 언급되고 풀이가 없다. 학부 3~4학년이 DNS SRV 레코드를 모를 수 있으며, CKA 시험에서 gRPC/서비스 포트 검색에 관련된다. → SRV 레코드 첫 등장 시 한 줄 풀이('SRV: 서비스 포트와 프로토콜을 DNS로 공개하는 레코드 타입. 예: _http._tcp.web-svc.ns.svc.cluster.local -> port 80 TCP')를 추가한다.
- **LOW** `용어비약` _학습 목표 (line 15)_ — '시험 패턴 12개 이상을 시간 내에 해결한다'는 목표가 제시되나 실제로는 미니랩 3개(7분)와 자가점검 Q5 외에 별도 '패턴 12개' 훈련 섹션이 없다. 목표와 본문 구성이 불일치한다. → 목표 문구를 실제 구성에 맞게 수정('YAML 예제 17개와 미니랩 3개를 통해 Service 생성/조회/수정 패턴을 손에 익힌다')하거나, 섹션 7의 예제 17개가 '시험 패턴'임을 명시적으로 연결한다.
- **LOW** `실습재현불가` _실습 3 (line 1238)_ — `kubectl get svc nginx -n demo ... | python3 -m json.tool` 명령은 busybox/alpine 기반 파드나 python 미설치 노드에서는 실행 불가하다. 또한 demo ns 에 nginx 서비스가 없을 때 실패한다. → 파이프를 `| python3 -m json.tool` 대신 `-o jsonpath` 출력 그대로 보여주거나, jq 사용 가능한 환경이면 `| jq .` 로 교체한다. python3 의존성은 학생 환경에 보장되지 않는다.
- **LOW** `스크린샷부족` _실습 1 '예상 출력' (line 1197)_ — day11-03-svc-types.png 앞에 '예상 출력 (예시 - fresh dev 클러스터에는 해당 서비스가 없다)' 라는 단서가 붙어있어 이 스크린샷이 실습 1 셋업 결과물이 아님을 고지했으나, 학생 입장에서 어떤 클러스터/환경에서 캡처된 것인지 맥락이 모호하다. → 캡션에 캡처 환경(예: '별도 데모 클러스터, demo ns 에 미리 배포된 여러 서비스')을 명시하거나, 실습 1에서 실제로 만든 cap-cka-d11 결과만 스크린샷으로 대표한다.

### daily/day12.md
- **HIGH** `구조` _## 섹션 번호 — 파일 전체_ — 섹션 번호가 '8. 시험에서...'로 시작해 1~7이 완전히 부재한다. CLAUDE.md 일별 양식은 '1. <소주제>' 구조를 요구하며, 독자는 앞 절이 빠진 것인지 의도된 것인지 알 수 없다. 학습 흐름 이후 바로 문제 풀이로 넘어가는 구성 근거가 없다. → 섹션 번호를 1, 2, ... 순서로 재정렬하거나, '이 day는 연습 문제 중심이므로 개념 절(1~7)은 day11에 위임한다'는 한 줄 안내를 '학습 흐름' 바로 밑에 추가해 구조 의도를 명시한다.
- **HIGH** `스크린샷부족` _문제 4·5·7·8·9·10·11·12 풀이 <details> 내부_ — 12개 연습 문제 중 8개(4, 5, 7, 8, 9, 10, 11, 12)에 '검증 기대 출력' 이미지가 없다. 문제 4(Endpoints 비어있음 진단)·11(종합 진단)처럼 가장 중요한 트러블슈팅 문제가 텍스트 주석(# ENDPOINTS: <none>)만 있고 실제 화면이 없다. CLAUDE.md 절대규칙 4-1에 위반된다. → 각 문제의 검증 단계(`kubectl get endpoints`, `kubectl get svc -o wide` 등) 실행 결과를 실제 터미널 캡처 PNG로 추가한다. 최소한 고배점 문제(4번 7%, 11번 7%)부터 우선 캡처한다.
- **HIGH** `정확성오류` _문제 2 풀이, 177번째 줄 이미지 캡션_ — 문제 조건은 'NodePort 31080'이고 YAML도 nodePort: 31080으로 작성했으나, 검증 이미지 캡션이 '80:31888'로 다른 포트를 표시한다. 학생이 자신의 실습 결과(31080)와 캡션(31888)이 달라 혼동할 수 있다. → 캡션을 'NodePort Service(80:31080) + Endpoints(web-app Pod 2개)'로 수정하거나, 실제 31080으로 캡처된 스크린샷으로 교체한다.
- **MED** `스크린샷부족` _tart-infra 실습 4 — NodePort 외부 접근 테스트_ — 실습 4(curl http://${DEV_WORKER_IP}:30080)에 curl 응답 출력 스크린샷이 없다. 실습 1~3은 이미지가 있는데 4만 빠져 있어 학생이 기대 결과를 확인할 수 없다. → dev 클러스터에서 curl 실행 결과(nginx HTML 일부 또는 200 응답)를 PNG로 캡처해 실습 4 bash 블록 아래에 삽입한다.
- **MED** `맥락점프` _tart-infra 실습 1 — '예상 출력 (예시)' 주석, 952번째 줄_ — 실습 1·2의 이미지 앞에 '예상 출력 (예시 — fresh dev 클러스터에는 demo ns ... 없다)' 텍스트가 붙어 있다. 학생이 따라하면 빈 결과가 나오는데, 교재 이미지는 demo 서비스가 있는 다른 환경 화면이다. 따라 하는 독자가 '내 결과와 다르다'는 혼동을 겪는다. → 실습 1~2 앞에 데모 서비스를 배포하는 최소 준비 명령(kubectl create deployment + expose for nginx-web, httpbin 등)을 추가하거나, 실습 목적을 'day12 전용 네임스페이스(cap-cka-day12)에서 직접 만든 리소스를 관찰'로 통일한다.
- **MED** `스토리라인누락` _문제 5(멀티 포트), 문제 9(타입 변경), 문제 10(kubectl expose 빠른 사용)_ — 멀티 포트 name 필수 이유는 풀이에 설명됐으나, 문제 9·10의 풀이에는 '왜 이 방법이 CKA 시험에서 유효한가'에 대한 등장 배경이 없다. CLAUDE.md 4겹 원칙(등장 배경 -> 개선 -> 트레이드오프) 중 배경이 빠졌다. → 문제 9 풀이에 'ClusterIP->NodePort 변경이 필요한 실전 시나리오(외부 노출 요구 발생)' 1~2문장 추가. 문제 10 풀이에 'kubectl expose vs YAML의 속도 트레이드오프' 요약 추가.
- **MED** `이해난이도` _문제 10 풀이, kubectl run api-server --port=8080_ — kubectl run에서 --port=8080은 containerPort 주석용이며 실제 리스닝 포트를 바꾸지 않는다. nginx 이미지는 80번 포트를 리스닝하므로 expose --port=8080으로 만든 ClusterIP Service로 접근하면 실패한다. 이 함정을 설명 없이 그냥 지나간다. → 풀이 내 주석 또는 핵심 포인트에 'kubectl run --port는 containerPort 힌트만 주고 실제 앱 포트를 바꾸지 않는다. 이미지 기본 포트(nginx=80)와 --port 값이 일치해야 한다'는 경고를 추가한다.
- **LOW** `용어비약` _문제 6 풀이, StatefulSet serviceName 필드_ — Headless Service와 StatefulSet을 함께 사용할 때 StatefulSet spec에 serviceName: redis-headless 필드가 등장하나, 이 필드가 왜 Headless Service 이름과 일치해야 하는지 설명이 없다. → StatefulSet YAML 옆 주석 또는 핵심 포인트에 'serviceName은 StatefulSet이 Pod DNS(<pod>.<serviceName>.<ns>...)를 등록할 때 사용하는 서비스 이름으로, Headless Service 이름과 반드시 일치해야 한다' 1줄을 추가한다.
- **LOW** `용어비약` _파일 첫머리 — kubectl 별칭 셋업 부재_ — CLAUDE.md는 실기 교재에서 'alias k=kubectl, export do=--dry-run=client -o yaml 셋업을 첫머리에 안내'하도록 명시하나, day12.md에 해당 내용이 없다. 12개 문제 풀이에서 모두 kubectl 풀이름을 쓰고 있어 시험 속도 훈련 의도가 약해진다. → 학습 목표 또는 시험 팁 섹션에 시험 시작 직후 실행할 셋업 명령(alias k=kubectl, source <(kubectl completion bash), export do='--dry-run=client -o yaml') 블록을 추가한다.

### daily/day13.md
- **HIGH** `실습재현불가` _실습 2: 네트워크 정책 동작 테스트 (line 1140-1153)_ — 허용/차단 비교 실습의 핵심인 'access=true 레이블을 가진 Pod를 별도로 띄워 허용되는지 확인'하는 명령이 없다. 제시된 두 kubectl run 명령은 모두 레이블 없는 nettest Pod를 띄우므로 deny-all+allow 정책 아래서 둘 다 차단된다. 스크린샷 캡션은 'access=true Pod는 허용, 무라벨 Pod는 차단'이라고 설명하지만 access=true Pod를 만드는 명령이 본문에 없다. → 실습 2 안에 'kubectl run nettest-allowed --image=busybox:1.36 -n demo --labels=access=true --rm -it --restart=Never -- wget ...' 명령을 추가하고, 무라벨 Pod(차단)와 access=true Pod(허용)의 결과를 대조하는 구성으로 수정한다.
- **MED** `실습재현불가` _시험 팁 > 시간 배분 및 학습 목표 (line 19, 1295)_ — 학습 목표에 '시험 패턴 12개 이상을 시간 내에 해결한다'고 명시하지만 실제로 시간 측정을 의식한 독립 미니랩은 Q11 1개뿐이다. 실전 예제 9개(섹션 8)는 YAML 템플릿이고 직접 작성 후 kubectl apply 검증까지 유도하지 않아 손 숙련 목표를 충족하지 못한다. → 섹션 8 예제 중 3-4개를 '제한 시간 N분 내에 명령형 또는 YAML로 만들고 kubectl apply 후 wget/curl로 검증하라'는 형식의 미니랩으로 전환한다. 각 랩에 예상 소요 시간을 명시한다.
- **MED** `실습재현불가` _tart-infra 실습 > 실습 환경 설정 (line 1055-1061)_ — CKA 실기 시험의 속도 기반 특성상 alias k=kubectl, export do='--dry-run=client -o yaml' 셋업이 문서 곳곳에서 전제로 쓰이지만(CLAUDE.md 규칙), 이 파일 어디에도 세션 시작 시 셋업 명령 블록이 없다. 학생이 이 파일만 보면 셋업 없이 긴 kubectl 명령을 반복 입력하게 된다. → 실습 환경 설정 블록에 'alias k=kubectl; complete -F __start_kubectl k; export do="--dry-run=client -o yaml"' 및 KUBECONFIG 설정을 추가한다.
- **MED** `실습재현불가` _시험 팁 > 시간 배분 (line 1295)_ — NetworkPolicy 문제에서 'kubectl create networkpolicy --help 또는 공식 문서 예제를 복사'를 권장하지만 kubectl create networkpolicy의 실제 문법(--pod-selector, --ingress-rule, --egress-rule 등)과 한계(Ingress/Egress 동시 지정 불가 등)를 한 번도 보여주지 않는다. 학생이 시험장에서 --help를 읽는 시간을 단축하려면 사전 숙지가 필요하다. → 섹션 6.9처럼 kubectl create networkpolicy의 명령형 예시(예: --pod-selector, --ingress-rule 파라미터)를 별도 소절로 추가하거나 시험 팁에 자주 쓰는 플래그를 표로 정리한다.
- **LOW** `구조` _섹션 7.1 CNI 플러그인 표 (line 748-755)_ — CNI 비교 표가 코드 블록 안 ASCII 박스 테이블로 작성됐고 ✅/❌ 이모지를 포함한다. CLAUDE.md 규칙 §4②는 다이어그램에 ASCII 박스를 금지하고 §5는 이모지 금지를 명시한다. 표 자체는 허용되나 이모지는 규칙 위반이다. → ASCII 박스 표를 GFM 마크다운 표(| 구분자)로 전환하고 ✅/❌를 'O/X' 또는 '지원/미지원' 텍스트로 대체한다.
- **LOW** `맥락점프` _실습 1 사전 구성 및 실습 1 (line 1063-1127)_ — 실습 1 사전 구성에서 demo ns에 nginx와 NetworkPolicy를 만들라고 안내하고, 바로 이어서 실습 1에서는 'cap-cka-day13 ns에 정책을 만들어 검증했다'는 주석이 등장한다. 독자는 본인이 따라야 할 ns(demo)와 작성자가 검증한 ns(cap-cka-day13)가 다르다는 점에 혼란을 느낄 수 있다. → 해당 주석을 '아래 스크린샷은 cap-cka-day13 ns에서 사전 캡처한 것이며, 독자는 demo ns에서 동일하게 재현할 수 있다'처럼 맥락을 명시하는 한 문장으로 수정한다.

### daily/day14.md
- **HIGH** `구조` _## 2. NetworkPolicy 다음 ~ ## 9. 사이_ — 섹션 번호가 2에서 9로 점프한다. 3~8번 섹션이 완전히 누락되어 있다. CLAUDE.md §5 daily 양식에 명시된 '실측 검증' 및 '직접 해보기' 서브섹션도 섹션 1, 2 모두 없다. → Ingress(## 1)와 NetworkPolicy(## 2) 각각에 '### 실측 검증'(kubectl get/describe + 스크린샷)과 '### 직접 해보기'(시험형 미니랩) 서브섹션을 추가하고, 섹션 번호를 3~8로 채우거나 누락을 명시적으로 통합해 번호 점프를 해소한다
- **HIGH** `스크린샷부족` _문제 2~5, 7~12 풀이 sections_ — 12개 문제 중 스크린샷이 있는 것은 문제 1과 문제 6 두 개뿐이다. 나머지 10개 문제에는 검증 출력이 전혀 없어 학생이 '내 결과가 맞는지' 확인할 수 없다. CLAUDE.md §4①은 명령 실행 결과를 스크린샷으로 제시하도록 규정한다. → 문제 2(allow-web-to-db describe), 문제 4(AND 조건 describe), 문제 5(api-egress describe), 문제 8(networkpolicy 목록), 문제 12(트러블슈팅 nc 실패->성공) 등 핵심 문제에 우선적으로 kubectl describe/get 출력 스크린샷을 캡처해 삽입한다
- **HIGH** `맥락점프` _## 1. Ingress(L24)와 ## 이 Day의 두 주제 > Ingress 등장 배경(L87)_ — 두 절이 거의 동일한 L4 한계->Ingress 필요성 서술을 반복한다. 분량 약 10줄 중복으로 문서 구조가 혼란스럽고 독자가 '이미 읽었는데 왜 또?' 라는 인식을 갖는다. → '이 Day의 두 주제' 절의 등장 배경 문단을 삭제하거나, 섹션 1.Ingress의 내용을 이 절로 통합하고 중복을 제거한다
- **MED** `실습재현불가` _tart-infra 실습 > 실습 1, 실습 2_ — 실습 1(CiliumNetworkPolicy 목록)과 실습 2(nginx->httpbin 통신 테스트) 모두 'fresh dev 클러스터에는 재현 불가'로 명시되고 이미지는 다른 환경의 참조 캡처다. 스크린샷은 존재하지만 학생이 같은 결과를 얻을 수 없어 실습 재현 목적이 달성되지 않는다. → fresh 클러스터에서 재현 가능한 대체 실습(예: 문제 1-12의 NetworkPolicy를 dev에서 직접 apply -> describe -> delete)을 실습 1,2 앞에 배치하거나, 재현 가능한 버전을 별도 '실습 A: fresh 환경'으로 추가한다
- **MED** `용어비약` _## 11. 복습 체크리스트 > 'IngressClass의 역할을 이해하는가?'_ — 체크리스트에 IngressClass 이해 여부가 포함되어 있으나, 본문 어디에도 'kubectl get ingressclass' 명령이나 IngressClass 오브젝트 자체(별도 CRD, defaultIngressClass 어노테이션)에 대한 설명이 없다. ingressClassName 필드 설명만 있어 체크리스트 항목과 본문 사이에 공백이 생긴다. → 예제 10 또는 시험 환경 설정 절에 'kubectl get ingressclass' 명령과 IngressClass 오브젝트(spec.controller 필드, is-default-class 어노테이션)를 한 단락으로 설명한다
- **MED** `이해난이도` _## 1. Ingress > 개념·내부 동작_ — Ingress 트래픽 흐름이 순서 5단계 텍스트로만 서술된다. CLAUDE.md §4② 는 개념·흐름 그림을 mermaid 흑백 다이어그램으로 표현하도록 규정하나 다이어그램이 없다. 마찬가지로 NetworkPolicy OR/AND 조건도 YAML 코드블록만 있고 흐름 다이어그램이 없다. → Ingress 트래픽 흐름(클라이언트->DNS->컨트롤러->Service->Pod)을 mermaid 흑백 sequenceDiagram 또는 flowchart LR로 추가한다. OR/AND 정책 논리도 mermaid flowchart로 시각화한다
- **LOW** `스토리라인누락` _## 2. NetworkPolicy > 등장 배경·스토리_ — '직전 기술과의 차이' 요소가 빠져 있다. CLAUDE.md §4④ 4요소 중 '이전 기술(예: iptables 수동 규칙, 외부 방화벽)의 한계'가 명시되지 않고 바로 NetworkPolicy 해법으로 넘어간다. → iptables 규칙 직접 관리의 한계(Pod IP가 재시작마다 바뀌어 규칙이 무의미해짐)를 한 문단으로 추가해 '직전 기술 비교' 요소를 채운다
- **LOW** `이해난이도` _문제 3 풀이 > monitoring 네임스페이스 레이블 추가 명령_ — OR 조건 예시에서 namespaceSelector의 레이블을 `purpose=monitoring`으로 임의 지정하는데, 실제 monitoring 네임스페이스에 이 레이블이 없을 경우 정책이 전혀 동작하지 않는다. 학생이 검증 명령(kubectl get ns monitoring --show-labels)을 실행하지 않으면 오류 원인을 파악하기 어렵다. → 문제 3 풀이에 'kubectl get ns monitoring --show-labels' 명령을 사전 확인 단계로 명시하고, kubernetes.io/metadata.name: monitoring 레이블(자동 부여)을 활용하는 방법도 병기한다

### daily/day15.md
- **HIGH** `스크린샷부족` _§실습 2(1151-1154행), §실습 3(1171-1172행)_ — day15-06(df 출력)과 day15-07(StorageClass describe)이 '참고 화면(다른 환경 형태)'으로 명시되어 있다. 학생이 자신의 dev 클러스터에서 실행했을 때 동일 포맷인지 보장이 안 되며, CLAUDE.md §4-1의 '실제 터미널 캡처' 요건을 충족하지 못한다. 특히 실습 2의 static-pod df -h 결과는 학생이 실습 1을 따라 해야 나오는 출력임에도 '다른 환경(PostgreSQL)'의 참고 화면으로 대체돼 있다. → dev 클러스터에서 실습 1의 static-pod를 실제로 실행하고 'kubectl exec -n demo static-pod -- df -h /usr/share/nginx/html' 출력을 캡처한 스크린샷으로 교체한다. 실습 3도 local-path provisioner 설치 후 'kubectl describe storageclass local-path' 출력을 dev 클러스터에서 캡처한 실제 화면으로 교체한다.
- **HIGH** `실습재현불가` _§8 미니랩(1192-1240행)_ — 미니랩이 2개뿐이고 Dynamic Provisioning(PVC 생성 -> WaitForFirstConsumer Pending -> Pod 생성 -> Bound 전환) 흐름을 직접 실행하는 미니랩이 없다. CKA 시험에서 StorageClass를 지정한 PVC를 만들고 Pod를 붙여 Bound로 전환시키는 문제가 출제되는데 이 흐름의 손 연습이 빠져 있다. → 미니랩 3을 추가한다: dev 클러스터에 local-path StorageClass가 있는 상태에서 PVC(storageClassName: local-path, 1Gi, RWO)를 생성해 Pending을 확인하고, nginx Pod를 붙여 WaitForFirstConsumer로 Bound가 되는 것을 'kubectl get pvc -w'로 확인하는 15분 실습. 실제 터미널 캡처를 첨부한다.
- **MED** `실습재현불가` _§2.3(122-123행), 그림 2(149행)_ — Retain 정책으로 PVC 삭제 시 Released 상태가 된 PV를 다시 Available로 복구하는 구체적 절차('kubectl edit pv <name>'으로 spec.claimRef 블록 삭제)가 다이어그램 엣지 라벨과 자가점검 Q2 한 줄에만 언급되고, 실제 YAML 편집 예시나 명령이 없다. 시험에서 'Released PV를 재사용 가능 상태로 만들라'는 태스크가 나올 때 학생이 막힌다. → §2.3 또는 §3.4 트러블슈팅에 Released->Available 복구 명령 예시를 추가한다: 'kubectl edit pv <name>' 후 spec.claimRef 필드 전체를 삭제하거나 'kubectl patch pv <name> -p {"spec":{"claimRef":null}}' 한 줄 명령으로 Available로 복구하는 방법을 코드 블록과 함께 보여준다.
- **MED** `스토리라인누락` _§5.6 projected 볼륨(551-588행)_ — projected 볼륨이 등장 배경 없이 YAML만 제시된다. 'ConfigMap·Secret·downwardAPI를 컨테이너마다 별도 volumeMounts로 나열해야 했던 복잡성을 하나의 마운트 경로로 통합하는 것이 목적'이라는 설명이 없어, 왜 이 볼륨 타입이 필요한지 학생이 이해하기 어렵다. → §5.6 앞에 2-3문장 등장 배경을 추가한다: 애플리케이션이 ConfigMap(설정)·Secret(인증정보)·Pod 메타데이터·ServiceAccount 토큰을 모두 파일로 참조해야 할 때 각각 별도 볼륨으로 관리하면 volumeMounts가 늘어나고 마운트 경로가 흩어진다는 문제, 그리고 projected가 이를 단일 디렉터리로 통합한다는 설명.
- **MED** `구조` _223행 그림 캡션_ — §3.2의 Dynamic Provisioning volumeBindingMode 다이어그램 캡션이 '_그림. ..._'로 번호가 없다. 앞에 그림 1·그림 2가 있어 이 다이어그램은 그림 3이어야 하지만 번호가 누락되어 인용 일관성이 깨진다. → 223행의 '_그림. Dynamic Provisioning ..._'을 '_그림 3. Dynamic Provisioning 의 PV 생성 시점(volumeBindingMode 별)._'으로 수정하고, 이후 다이어그램도 순번을 이어서 부여한다.
- **LOW** `용어비약` _§6.4 볼륨 권한 설정(684-708행)_ — fsGroup이 '마운트된 볼륨의 그룹 ID'라는 주석만 있고 동작 메커니즘(Kubelet이 마운트 후 chown으로 파일 소유 그룹을 fsGroup 값으로 변경)이 없다. 컨테이너 runAsUser/runAsGroup과 fsGroup의 차이를 모르는 학부생은 왜 두 레벨의 securityContext가 필요한지 이해하기 어렵다. → fsGroup 설명에 1-2문장을 추가한다: Kubelet이 볼륨을 마운트한 직후 해당 디렉터리 전체에 chown :fsGroup을 적용해 컨테이너 프로세스(runAsGroup 기준)가 그 파일을 읽고 쓸 수 있도록 한다. runAsUser/runAsGroup은 프로세스 UID/GID이고, fsGroup은 마운트된 스토리지 파일의 소유 그룹을 강제하는 별도 메커니즘이다.
- **LOW** `맥락점프` _실습 1 안내(1125-1128행)_ — 실습 1 본문이 'fresh dev 클러스터에서 재현하려면 §7 예제 1을 적용하면 된다'라고 쓰고 바로 day15-05 스크린샷을 제시하는데, 예제 1 YAML을 어떤 파일명으로 저장해 어떤 명령으로 적용하는지 연결 명령이 없다. 학생이 §7을 찾아와 YAML을 복사해 적용해야 하는데 그 경로가 명시되지 않아 흐름이 끊긴다. → day15-05 스크린샷 앞에 'kubectl apply -f static-pv.yaml' 등 구체적인 적용 명령 코드 블록을 1-2줄 삽입하고, 적용 후 'kubectl get pv,pvc -n demo'로 상태를 확인하는 명령을 추가해 흐름을 이어 준다.

### daily/day16.md
- **HIGH** `스크린샷부족` _문제 3(emptyDir Multi-Container), 문제 7(PVC Pending 진단), 문제 8(여러 볼륨 타입), 문제 9(Retain/Released/Available), 문제 10(subPath), 문제 11(Dynamic Provisioning), 문제 12(storage-info.txt)_ — 12문제 중 7문제(3, 7, 8, 9, 10, 11, 12)에 '검증 기대 출력' 스크린샷이 없다. 특히 문제 7의 진단 단계(PVC Events, PV storageClassName 불일치)와 문제 9의 Released -> Available 상태 전이는 시각 확인이 학습 효과의 핵심인데 텍스트 주석(# STATUS: Released)으로만 대체돼 있다. → 문제 3, 7, 8, 9, 10, 11, 12 각 풀이의 최종 검증 명령 출력을 실제 터미널에서 캡처해 images/day16-07-emptydir.png 등으로 삽입하고 '검증 기대 출력:' 블록을 추가한다. 문제 7은 describe pvc 이벤트 화면과 수정 후 Bound 상태를 각각 별도 스크린샷으로 제시한다.
- **HIGH** `맥락점프` _예제 13(initContainer 볼륨 초기화), 예제 14(hostPath 소켓 마운트)_ — 두 예제 모두 도입 산문 없이 YAML 코드블록이 바로 시작된다. 예제 13은 '왜 initContainer로 볼륨을 초기화하는가(앱 컨테이너 기동 전 파일이 있어야 하는 이유)'가 없고, 예제 14는 '왜 docker.sock을 마운트하는가(CI/DinD 사례)'에 대한 설명이 없다. 예제 12에는 한 단락 도입문이 있지만 13, 14에는 없어 흐름이 끊긴다. → 예제 13 앞에 'initContainer는 앱 컨테이너 시작 전 실행되는 초기화 전용 컨테이너다. emptyDir과 조합하면 앱 컨테이너가 필요로 하는 파일/디렉터리를 미리 준비할 수 있다(예: DB 마이그레이션, 설정 파일 생성).' 2~3문장을 추가한다. 예제 14 앞에 'hostPath 소켓 마운트는 Pod에서 호스트의 컨테이너 런타임에 직접 접근할 때 쓴다. CI 파이프라인이 빌드 에이전트 Pod에서 docker 명령을 실행해야 할 때가 대표 사례다.' 설명을 추가한다.
- **HIGH** `맥락점프` _예제 14 직후, 학습 목표 3번 '보안 주의점'_ — 학습 목표에 'hostPath 볼륨의 보안 주의점을 설명할 수 있다'고 명시했지만 예제 14 본문 어디에도 보안 위험(소켓 마운트 = 컨테이너가 호스트 런타임 제어권 획득 -> 컨테이너 탈출 가능, 프로덕션 금지)이 없다. YAML 주석 한 줄('호스트의 컨테이너 런타임 접근')만 있어 위험성을 모르고 그냥 쓸 수 있다. → 예제 14 YAML 블록 직후에 '[보안 주의] docker.sock을 마운트한 컨테이너는 호스트의 모든 컨테이너를 실행/삭제하고 privileged 컨테이너를 띄울 수 있다. 이는 사실상 호스트 root 권한과 동등하다. 프로덕션 클러스터에서는 PodSecurity Admission이나 OPA/Gatekeeper로 hostPath 소켓 마운트를 차단한다. tart-infra 실습에서는 dev/staging에서만 사용한다.' 경고 blockquote를 추가한다.
- **MED** `실습재현불가` _tart-infra 실습 1, 2 (PostgreSQL/Redis 전제), 실습 4 (Istio+httpbin 전제)_ — 실습 1, 2는 'dev 클러스터에 PostgreSQL이 배포된 환경' 전제이고, 실습 4는 'Istio와 httpbin이 배포된 환경' 전제다. fresh dev 클러스터에서는 재현 불가라고 명시돼 있지만, '앱을 먼저 배포한 뒤'라고만 하고 배포 방법(kubectl apply 명령 또는 링크)이 없다. 학생이 단독으로 실습을 따라 할 수 없다. → 실습 1, 2 앞에 PostgreSQL을 demo ns에 빠르게 배포하는 미니 manifest(Deployment+PVC or StatefulSet)를 직접 제공하거나, manifests/ 디렉터리의 기존 예제를 구체적으로 링크한다. 실습 4 대안 명령(sidecar-pod 대체)은 이미 있으므로 그 대안을 기본으로 격상시키고 Istio 전제를 부록으로 이동한다.
- **MED** `스크린샷부족` _tart-infra 실습 3(ConfigMap 볼륨 마운트 확인), 실습 5(StorageClass 확인)_ — '(미캡처) — 캡처 예정'으로 표시돼 실습 결과 화면이 없다. CLAUDE.md §4①에서 미캡처 시 '(미캡처)'로 명시하도록 규정하고 있어 규칙은 지켰으나, 학생 입장에서는 명령 출력이 어떻게 나와야 정상인지 알 수 없다. → dev 클러스터 기동 후 실습 3, 5의 명령을 실행해 터미널 출력을 캡처한다. 실습 3은 문제 4에서 이미 nginx-custom Pod를 만들었으므로 해당 상태에서 캡처가 용이하다. 실습 5는 `kubectl get storageclass` 결과 한 줄짜리 출력이므로 즉시 캡처 가능하다.
- **MED** `용어비약` _예제 15 volumeName 직접 지정, '0. 이번 Day의 위치와 흐름' 바인딩 조건 표의 selector 행_ — 예제 15 직전에 volumeName 필드를 설명하는 문장이 없다. YAML 주석 '# 이 특정 PV에 바인딩'만 있어 왜 이 방식을 쓰는지(selector matchLabels 대신 volumeName으로 직접 지정하는 차이)가 불명확하다. 또한 selector 조건 설명에서 '라벨이 매칭돼야 함(선택적)'이라고만 해서 matchLabels와 matchExpressions의 차이가 없다. → 예제 15 앞에 'volumeName은 selector/matchLabels 없이 PVC가 특정 PV 하나를 이름으로 직접 지목하는 방식이다. selector는 라벨로 여러 PV 중 하나를 선택하는 반면 volumeName은 이름으로 단 하나를 고정한다. 동일 storageClassName 조건은 여전히 필요하다.' 설명을 추가한다.
- **LOW** `구조` _tart-infra 실습 섹션 전체 위치 (## 내일 예고 뒤, 파일 끝부분)_ — tart-infra 실습이 '내일 예고' 다음에 위치해 마치 부록처럼 보인다. 학생이 문제 12를 풀고 '다음은 Day 17' 안내를 읽은 후 스크롤을 더 내려야 실습 섹션이 있음을 알게 된다. CLAUDE.md §5의 일별 양식('실측 검증' -> '직접 해보기' -> '복습 체크리스트' -> '자가점검' -> '시험 팁' -> '더 읽을거리')과 구조가 역전돼 있다. → tart-infra 실습 섹션을 '더 읽을거리' 앞, 복습 체크리스트 뒤로 이동하거나, 최소한 '내일 예고' 앞으로 이동해 독자가 파일 끝까지 읽어야 실습이 있음을 인지하게 한다.
- **LOW** `이해난이도` _문제 6, 문제 12 platform 클러스터 전제_ — 두 문제 모두 platform 클러스터에 kube-prometheus-stack이 배포된 환경을 전제로 한다. 대안(staging/dev 대체)이 제시돼 있으나 대안 자체도 '문제 1 풀이 결과로 생성된 PVC'가 남아 있다는 부가 조건이 있다. 문제를 순서대로 풀다가 정리(kubectl delete) 단계를 실행했으면 PVC가 없어 대안도 재현 불가하다. → 문제 6, 12의 대안 섹션에 platform PVC가 없는 경우를 독립적으로 재현하는 미니 setup 명령(PVC 하나를 즉석 생성하는 두 줄)을 추가해 대안이 완전 자급자족하도록 한다.

### daily/day17.md
- **HIGH** `실습재현불가` _## 직접 해보기 — 시험형 미니랩 > 문제 1 (5분)_ — 셋업 블록은 lab17 네임스페이스 생성만 한다. 문제 1은 '현재 비정상 상태인 Pod를 찾아라'고 요구하는데, 비정상 Pod를 생성하는 명령이 없다. 학생이 그대로 따라 하면 lab17이 비어 있어 문제 자체가 성립하지 않는다. → 셋업 블록에 CrashLoopBackOff Pod 생성 명령(예: kubectl run crash-lab -n lab17 --image=busybox:1.36 --command -- sh -c 'exit 1')을 추가한다.
- **HIGH** `스크린샷부족` _## 3. Pod 상태별 진단 가이드 > 3.2 Pending Pod 진단, 3.3 CrashLoopBackOff 진단, 3.5 OOMKilled 진단 / ## 4. Node 문제 진단 > 4.2 kubelet 장애 복구_ — 핵심 진단 섹션 4개(Pending describe 출력, CrashLoopBackOff logs --previous 출력, OOMKilled describe 출력, systemctl status kubelet 출력)가 yaml 블록 내 주석(# NAME...)으로만 표시되고 실제 스크린샷이 없다. CLAUDE.md 절대규칙 4-1을 위반한다. → 각 섹션에 실제 dev 클러스터에서 캡처한 스크린샷(day17-01-pending.png, day17-kubelet-status.png 등)을 삽입한다. 캡처가 불가능하면 '(미캡처)'로 명시한다.
- **HIGH** `스크린샷부족` _## 8. 실전 장애 시나리오 > 시나리오 1~15 전체_ — 15개 시나리오 모두 '진단' 결과를 yaml 주석(# STATUS: ImagePullBackOff, # Events: ...) 형태로만 표기하고 실제 터미널 스크린샷이 하나도 없다. 학생 입장에서 '실제로 이렇게 보이는구나'를 확인할 수 없다. → 시나리오 1(ImagePullBackOff)과 시나리오 8(Endpoints 비어있음) 최소 2개 이상에 실제 describe/get 출력 스크린샷을 추가한다. 나머지는 '(미캡처)' 명시.
- **MED** `용어비약` _## 2.2 핵심 진단 명령어 총정리 > kubectl top 항목_ — kubectl top nodes/pods 명령을 소개하지만 metrics-server가 설치돼 있어야 동작한다는 전제 조건을 언급하지 않는다. tart 클러스터에 metrics-server가 없으면 'error: Metrics API not available' 오류가 나는데, 학생은 이유를 모른다. → kubectl top 명령 바로 아래에 한 줄 주석으로 '# 전제: metrics-server 설치 필요. 없으면 kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml' 를 추가한다.
- **MED** `맥락점프` _## 5. Control Plane 컴포넌트 장애 > 5.1 컴포넌트별 장애 증상 > etcd 항목_ — 표에 etcdctl endpoint health 명령이 등장하지만 etcd 장애 진단의 실제 절차(etcdctl 환경변수 설정, ETCDCTL_API=3, --cacert/--cert/--key 경로 등)가 없다. 학생이 etcdctl을 처음 쓰면 인증서 없이 실행해 connection refused 오류를 받는다. → 5.1 표 아래 또는 별도 5.4 절에 etcdctl 사용을 위한 환경변수 설정 예시와 endpoint health 실행 방법을 추가한다.
- **MED** `이해난이도` _## 8. 실전 장애 시나리오 > 시나리오 11: CoreDNS CrashLoopBackOff_ — 시나리오 11의 '장애 생성' 단계가 'kubectl -n kube-system edit configmap coredns에서 Corefile에 문법 오류 추가'라고만 설명한다. 어떤 오류를 어디에 넣어야 재현되는지 구체적인 예(예: 중괄호 빠뜨리기, 잘못된 플러그인 이름)가 없어 학생이 직접 실습할 수 없다. → 문법 오류 예시(예: errors 플러그인을 erros로 오타 내거나 닫는 중괄호 삭제)를 heredoc 또는 구체 diff 형태로 제시한다.
- **LOW** `스토리라인누락` _## 6. DNS 문제 진단 / ## 7. Service 연결 문제 진단_ — 6절(CoreDNS)과 7절(Service)에는 등장 배경이나 '왜 이 도구/개념이 필요한가' 서술이 없다. 다른 섹션(3.4, 5.3)에는 '등장 배경' 헤딩이 있는데 6~7절은 바로 진단 절차로 시작한다. → 각 절 앞에 2~3문장 수준의 등장 배경(CoreDNS가 kube-dns를 대체한 이유, Service가 Endpoint 오브젝트를 통해 Pod를 참조하는 방식)을 추가한다.
- **LOW** `구조` _## tart-infra 실습 > 실습 1 ~ 3_ — 실습 1~2의 '예상 출력' 캡션에 'fresh dev 클러스터에는 demo ns의 nginx Pod가 없어 재현 불가'라고 명시돼 있다. 그런데 같은 섹션 상단 셋업 블록이 healthy-nginx Pod를 생성하도록 안내한다. 셋업을 먼저 실행한 학생에게 '재현 불가' 캡션은 혼란을 준다. → 캡션을 '(셋업 블록 실행 후 정상 env 기준)'으로 수정하거나, 셋업 블록과 이미지 캡션의 전제 조건을 일치시킨다.

### daily/day18.md
- **HIGH** `스크린샷부족` _문제 2, 3, 4, 5, 6, 7, 8, 10, 11 풀이(details) 내부 - 총 12문제 중 9문제에 검증 스크린샷 없음_ — CLAUDE.md 절대 규칙 4-1: 명령 실행 결과는 실제 터미널 스크린샷만 허용, 텍스트 블록 금지. 그런데 9개 문제의 풀이는 kubectl get/describe/logs 출력을 주석(# STATUS: Pending 등)으로만 표시하고 스크린샷이 전혀 없다. 7%짜리 고비중 문제(3, 6, 10, 11, 12)도 포함된다. → 각 문제 복구 완료 시점의 kubectl get pod/nodes/endpoints 출력을 실제 tart 클러스터에서 캡처해 images/day18-03-node-ready.png 형식으로 삽입. 최소한 문제 3(NotReady->Ready), 4(Endpoints 채워짐), 6(crictl ps Running), 11(READY 2/2), 12(전체 복구)는 필수.
- **HIGH** `실습재현불가` _문제 7(DNS 문제 해결, 4%) 풀이_ — 다른 11개 문제는 모두 '장애 생성' 블록으로 실제 고장을 만든 뒤 진단한다. 문제 7은 장애 생성 단계가 없다. 클러스터가 정상이면 CoreDNS가 정상이므로 '문제를 찾고 수정하라'는 시나리오 자체가 재현되지 않는다. → kubectl -n kube-system scale deployment coredns --replicas=0 으로 DNS를 고장 내거나, CoreDNS ConfigMap에 문법 오류를 삽입해 장애를 생성한 뒤 진단-복구 순서로 재구성한다. 복구 후 nslookup 성공 스크린샷 첨부.
- **MED** `스크린샷부족` _tart-infra 실습 1,4,5(lines 944, 994, 1013) - 스크린샷 캡션이 '예상 출력(예시)'_ — 세 곳 모두 '예상 출력(예시 - fresh dev 클러스터에는 이 앱들이 없다. 아래는 해당 앱이 배포된 환경의 형태)'라는 단서를 달고 있다. CLAUDE.md 정의 기준으로 이는 '참조 예시'이지 '실제 클러스터 캡처'가 아니다. 학생이 자기 환경에서 실행하면 다른 출력이 나와 혼란이 생긴다. → 실습 1(kubectl get pods -n demo -o wide)은 문제 섹션에서 직접 만드는 pending-app/crash-app을 demo ns에 두고 캡처하거나, '이 실습은 day11/day12의 워크로드가 있을 때 의미 있으며 없으면 빈 출력이 정상'임을 명시한다. 실습 5(kubectl top node)는 metrics-server 미설치 환경의 에러 출력 스크린샷을 추가로 포함시킨다.
- **MED** `맥락점프` _문제 4(Service 연결, line 337~402) 풀이 - 내부 동작 설명 누락_ — 장애가 selector 불일치 + targetPort 불일치 두 개인데, 각각이 Endpoints에 미치는 영향이 다르다. selector 불일치 -> Endpoints에 Pod IP 자체가 안 들어감. targetPort 불일치 -> Endpoints에 Pod IP는 들어오지만 실제 포트에 연결 실패. 두 원인의 차이와 patch 한 줄로 둘 다 고치는 이유를 설명하지 않아 학생이 '왜 patch가 두 필드를 동시에 수정하는가'를 이해하지 못한 채 명령만 외우게 된다. → 풀이 끝에 '핵심' 블록을 추가해 selector 불일치는 kube-controller-manager의 EndpointSlice controller가 라벨 매칭을 못해 Endpoints 자체가 비는 것이고, targetPort 불일치는 Endpoints에 IP는 있지만 트래픽이 엉뚱한 포트로 가는 별개의 문제임을 1~2문장으로 구분 설명한다.
- **MED** `이해난이도` _문제 11(Multi-Container Pod, line 715~724) 수정 코드 - tail -f /logs/access.log_ — 수정된 sidecar가 'tail -f /logs/access.log'를 실행하는데, emptyDir에 nginx가 처음 파일을 쓰기 전에는 access.log가 존재하지 않는다. busybox 1.36의 tail -f는 파일이 없으면 즉시 exit 1로 종료되어 초기에 CrashLoopBackOff가 다시 발생할 수 있다. 학생이 실행했을 때 여전히 Crash가 발생하면 혼란스럽다. → command를 'sh -c "while [ ! -f /logs/access.log ]; do sleep 1; done && tail -f /logs/access.log"' 로 수정해 파일 생성을 대기하게 하거나, 'tail -f /logs/access.log 2>/dev/null || sleep 3600' 처럼 안전한 대안 명령을 사용한다. 수정 이유를 한 줄 주석으로 설명한다.
- **MED** `용어비약` _문제 1 검증 기대 출력 캡션 및 문제 9 검증 기대 출력 캡션(lines 182, 599)_ — '검증 기대 출력'이라는 표현은 '실제 터미널 캡처'인지 '예상 결과 텍스트'인지 학생 입장에서 불분명하다. CLAUDE.md는 실제 캡처만 허용하므로 캡션이 실제 캡처임을 명시해야 한다. → 캡션을 '실제 터미널 캡처 - nodeSelector 제거 후 Pod Running 복구' 형식으로 변경해 실제 클러스터에서 촬영한 화면임을 명확히 한다.
- **MED** `구조` _문서 전체 - 자가점검 및 더 읽을거리 섹션 누락_ — CLAUDE.md daily 양식에는 '자가점검(<details> 정답)' 및 '더 읽을거리' 섹션이 필수다. 현재 파일에는 '복습 체크리스트'(section 6)가 있지만 <details> 정답 형식이 아닌 체크박스만 나열돼 있다. 스스로 답을 맞춰보는 검증 단계가 없다. → 섹션 6을 '## 자가점검' 으로 개편하고, 체크박스 항목마다 <details><summary>정답</summary> 블록으로 핵심 답변을 넣는다. 마지막에 '## 더 읽을거리'를 추가해 공식 K8s 문서(Troubleshooting 절, crictl 사용법 페이지) 링크를 연결한다.
- **LOW** `스토리라인누락` _문제 3(Node NotReady) 풀이 내부 - journalctl 출력 해석 설명 없음_ — journalctl -u kubelet 실행 명령은 있지만 '출력에서 무엇을 찾아야 하는가'를 설명하지 않는다. 이 실습은 kubelet을 수동으로 stop했으므로 journalctl에는 단순히 'Stopped kubelet'만 찍힌다. 실제 시험에서는 인증서 만료나 설정 오류 메시지가 찍히는데, 그 차이를 설명하지 않으면 학생이 'journalctl에서 뭘 봐야 할지' 모른다. → 풀이 끝에 '핵심' 블록을 추가해 journalctl 출력에서 주목할 패턴(certificate expired, failed to parse config, failed to connect to CRI endpoint 등)을 열거하고, 이 실습에서는 단순 정지라 그 메시지가 없지만 시험 환경에서는 이 패턴을 보고 원인을 특정하는 연습을 따로 해야 한다고 안내한다.
- **LOW** `이해난이도` _문제 6(kube-apiserver 복구) 풀이 - sleep 40 사용_ — CLAUDE.md에서 sleep 연쇄 대신 until 루프나 kubectl wait를 쓰도록 명시한다. sleep 40은 실행 환경에 따라 충분하지 않을 수 있고, 학생이 얼마나 기다려야 하는지 판단하지 못한다. → sleep 40 을 'until sudo crictl ps | grep -q apiserver; do sleep 3; done' 또는 'kubectl wait --for=condition=Ready pod -l component=kube-apiserver -n kube-system --timeout=120s' 로 교체한다.
- **LOW** `스토리라인누락` _문제 10(Control Plane 컴포넌트 진단, 7%) - 장애 생성 없음_ — 문제 10은 '상태 점검 및 정보 수집'이 목적이므로 장애 생성이 없는 건 맞다. 그러나 7%짜리 고비중 문제임에도 '왜 이 정보를 수집하는가'(모니터링/운영 증거 수집 목적), 그리고 Custom Columns 출력이나 jsonpath 파이프가 실제로 어떤 형태로 나오는지 스크린샷이 전혀 없다. 학생이 명령을 실행해도 출력이 예상과 다를 때 확인할 기준이 없다. → platform 클러스터에서 명령을 실제 실행한 스크린샷을 추가하고, /tmp/cp-status.txt의 내용 cat 출력 캡처도 포함한다. '왜 이 정보가 필요한가' 한 문장(운영 이슈 보고 시 Control Plane 정상 증거로 제출)을 추가한다.

### daily/day19.md
- **HIGH** `스크린샷부족` _문제 1~6, 10번 및 tart-infra 실습 1~2 — images/day19-01~07,09.png 8개 참조_ — 마크다운에서 참조하는 images/day19-0*.png 8개 파일이 images/ 디렉터리에 실제로 존재하지 않는다. 렌더링 시 broken image가 뜨며 '실측 증거'가 전혀 없다. → 실제 tart 클러스터에서 각 명령을 실행하고 scripts/capture-shot.sh 로 터미널 화면을 캡처해 images/day19-0*.png 로 저장한 뒤 마크다운이 가리키는 경로와 일치시킨다.
- **HIGH** `스크린샷부족` _문제 3(Static Pod), 7(PV/PVC), 8(Taint), 9(drain), 11(Ingress), 12(Job), 13(클러스터 정보), 14(트러블슈팅), tart-infra 실습 3_ — 위 9개 섹션은 '검증 기대 출력' 항목도, 이미지 참조 태그도 전혀 없다. 학생은 명령이 올바르게 동작했을 때 어떤 출력이 나오는지 확인할 수단이 없다. → 각 섹션에 '검증 기대 출력:' 항목을 추가하고 실측 스크린샷을 삽입한다. 최소 kubectl get 결과(PVC Bound 상태, Pod Running, Job COMPLETIONS, drain 후 SchedulingDisabled 등)를 캡처한다.
- **HIGH** `정확성오류` _문제 8(Taint/Toleration) — kubectl config use-context prod_ — Taint를 prod-worker2 에 추가하는 파괴 실습을 prod 클러스터에서 수행한다. CLAUDE.md S3 정책은 prod를 '읽기 위주, 데모 외 변경 자제'로 규정하며 파괴 실습은 dev/staging에서만 허용한다. → 컨텍스트를 dev 또는 staging 으로 변경하고, 노드 이름을 해당 클러스터 노드(dev-worker1 등)로 교체한다.
- **MED** `실습재현불가` _tart-infra 실습 1 — for ctx in platform dev staging prod 루프_ — 문서 자체가 '이 저장소의 4개 kubeconfig 는 컨텍스트 이름이 모두 kubernetes-admin@kubernetes 라 위 루프는 동작하지 않는다'고 인정하면서도 올바른 대체 명령 예시를 제공하지 않는다. 학생이 실습 1을 그대로 따라 하면 실패한다. → 동작하지 않는 루프 대신 --kubeconfig kubeconfig/<c>.yaml 을 각각 명시하는 4줄 명령 또는 올바른 컨텍스트 rename 방법(kubectl config rename-context)을 코드블록으로 제시한다.
- **MED** `스토리라인누락` _문제 3(Static Pod), 10(DaemonSet), 12(Job)_ — 문제 3과 10은 '문제 의도' 설명조차 없고, 등장 배경(왜 Static Pod/DaemonSet/Job이 필요한가, 직전 방식의 한계)이 완전히 누락됐다. 모의시험 특성상 반복 설명을 줄이는 것은 이해되나 이 세 문제는 처음 접하는 학생이 개념 부재 상태로 풀이만 암기하게 된다. → 각 문제 풀이 상단에 2~3줄 '문제 의도'와 링크('개념은 dayN 참조')를 추가한다. 전체 배경 재서술 대신 핵심 한 줄 + 해당 day 링크 패턴으로 분량을 최소화한다.
- **MED** `실습재현불가` _문제 3 풀이 — sudo cat <<EOF > /etc/kubernetes/manifests/static-busybox.yaml_ — sudo 와 heredoc 리다이렉트(> )를 조합하면 리다이렉트는 sudo 권한 없이 현재 사용자 권한으로 실행돼 Permission denied 가 발생한다. 실제 root 권한이 있는 환경이 아니면 재현 불가. → sudo tee /etc/kubernetes/manifests/static-busybox.yaml <<EOF ... EOF 패턴으로 교체한다. 이 방법은 tee 가 sudo 권한으로 파일을 쓰므로 안전하다.
- **MED** `스크린샷부족` _문제 7(PV/PVC), 문제 9(drain/uncordon)_ — PVC Bound 상태 확인(kubectl get pvc)과 drain 후 SchedulingDisabled 상태(kubectl get nodes)는 시험 채점 기준에 직결되는 검증 출력인데 이미지가 전혀 없다. 학생이 자신의 출력이 맞는지 비교할 기준이 없다. → kubectl get pv exam-pv / kubectl get pvc exam-pvc (STATUS=Bound 표시) 실측 캡처를 문제 7에, kubectl get nodes (SchedulingDisabled 표시) 실측 캡처를 문제 9에 추가한다.
- **LOW** `용어비약` _문제 3 풀이 — mirror Pod_ — mirror Pod 라는 용어가 괄호 속 '(읽기 전용)' 한 줄로만 언급된다. Static Pod가 왜 mirror Pod를 만드는지, API Server에서 어떻게 보이는지, 왜 kubectl delete로 지워지지 않는지 설명이 없다. → mirror Pod 첫 등장 시 '= kubelet이 API Server에 자동 등록하는 읽기 전용 복사본으로, kubectl delete로 지우면 kubelet이 즉시 재생성한다'는 한 줄 풀이를 추가한다.
- **LOW** `맥락점프` _문제 11(Ingress) — Ingress Controller 전제_ — Ingress YAML 적용 후 실제 동작하려면 클러스터에 Ingress Controller(예: nginx-ingress)가 설치돼 있어야 한다. 이 저장소 클러스터에 Ingress Controller가 설치돼 있는지 언급이 없어 학생이 apply 후 ADDRESS 가 비어 있어도 무엇이 잘못된 건지 알 수 없다. → 풀이 상단에 '이 저장소 dev 클러스터에서는 Ingress Controller 설치 여부를 kubectl get pods -n ingress-nginx 로 먼저 확인한다. 없으면 ADDRESS 가 비어 있어 실제 트래픽 테스트는 불가하다(CKA 시험은 Controller가 설치돼 있음)'는 한 줄 전제를 추가한다.

### daily/day20.md
- **HIGH** `정확성오류` _채점 기준 표 및 도메인별 배점 분석 표_ — 25문제 배점 합산이 본문에서 131%라고 명시됐으나 실제 합산은 136%(4*13 + 7*12 = 52+84=136)이다. 도메인별 배점 분석 표도 Workloads를 '5문제 / 26%'로 표기했으나 실제로는 Q4,Q8,Q10,Q12,Q16,Q18 6문제 / 30%이며, 문제 17(Multi-Container Pod)이 본 표에서는 Troubleshooting으로 분류됐으나 CKA 시험 도메인 기준으로는 Workloads & Scheduling에 해당한다. → 배점 합산을 136%로 수정하고 도메인별 분석 표의 Workloads 행을 '6문제 / 30%'로 교정한다. Q17 도메인을 Workloads로 이동하거나 도메인 분류 기준(CKA blueprint)을 명시한다.
- **HIGH** `스크린샷부족` _문제 17(Multi-Container Pod), 문제 20(로그 확인), 문제 21(etcd 복원), 문제 24(노드 NotReady 진단)_ — 4개 문제(배점 7+4+7+7 = 25%) 풀이에 검증 기대 출력 이미지가 없다. CLAUDE.md 절대규칙(§4①)은 명령 실행 결과를 실제 터미널 스크린샷으로 제시하도록 요구한다. 특히 문제 17은 7% 고배점이고 문제 24는 SSH 명령 시퀀스가 길어 학생이 중간 출력 형태를 확인할 수 없다. → 각 문제에 검증 기대 출력 스크린샷(images/day20-10~13)을 추가한다. 클러스터 미가동 상황이면 '(미캡처)'로 명시하되 텍스트 출력 블록 대체는 금지한다.
- **HIGH** `정확성오류` _문제 21 etcd 복원 풀이 bash 코드 블록_ — etcdctl snapshot restore 명령에 --name, --initial-cluster, --initial-cluster-token, --initial-advertise-peer-urls 파라미터가 누락돼 있다. kubeadm 환경에서 etcdctl v3 restore는 이 인자들 없이도 동작하지만, 인자를 생략하면 기본값(default, default=http://localhost:2380, etcd-cluster-1)이 사용된다는 설명이 없다. 또한 etcd.yaml 수정 시 volumeMounts의 mountPath도 변경해야 하는데, 그 부분이 주석에서 누락됐다('hostPath만 변경하면 됨'이라고 시험 팁에 명시하나, 실제로는 volumes[].hostPath.path와 volumeMounts[].mountPath 둘 다 또는 volumes만 변경해야 하는지 기술적으로 불분명하다). → kubeadm 기본 환경에서 restore 명령의 기본값이 적합한 이유를 한 줄로 설명하고, etcd.yaml 수정 시 volumes.hostPath.path만 변경하면 되는 이유(mountPath는 그대로이고 hostPath만 가리키는 물리 경로가 바뀌는 구조)를 1~2문장으로 명확히 한다.
- **MED** `구조` _파일 상단 (문제 15 이전)_ — 모의시험 Part 2 시작 시 시험 환경 셋업 블록(alias k=kubectl, export do='--dry-run=client -o yaml', KUBECONFIG 설정)이 없다. 셋업은 실습 3 섹션(line 931)에야 등장한다. 학생이 문제 15부터 바로 실습할 때 컨텍스트가 단절된다. Day 19와 이어진다고 가정하지만 이 파일만 읽는 학생은 셋업 없이 진입한다. → 파일 상단(> CKA 도메인 헤더 바로 아래)에 '시험 시작 전 환경 설정' 코드 블록(alias/KUBECONFIG/컨텍스트 초기화 3줄)을 추가하고 Day 19와의 연속성을 한 줄로 명시한다.
- **MED** `스토리라인누락` _문제 16(Affinity), 문제 19(ServiceAccount), 문제 20(로그 확인), 문제 22(cordon), 문제 23(Headless Service), 문제 24(노드 NotReady), 문제 25(kubectl top)_ — 7개 문제에 '등장 배경' 절이 없다. 문제 16은 '필드 이름 읽는 법'만 있고, Q19/Q22/Q23/Q24/Q25는 '문제 의도'로 바로 진입한다. CLAUDE.md §4④는 직전 기술의 한계, 무엇이 나아졌나, 트레이드오프의 4요소를 모든 주제에 명시하도록 요구한다. → 각 문제 풀이 상단에 해당 리소스가 왜 도입됐는지(Q16: Affinity 없던 시절 nodeSelector만 있을 때의 한계, Q22: drain과의 차이로 cordon이 필요한 운영 시나리오, Q24: 노드 장애 진단 프로세스가 표준화된 이유 등) 3~5문장의 등장 배경을 추가한다.
- **MED** `정확성오류` _문제 21 동작 원리 코드 블록 (line 395-411)_ — 동작 원리가 mermaid 다이어그램이 아니라 backtick 없는 텍스트 코드 블록(` ``` ` 언어태그 없이)으로 표현됐다. CLAUDE.md §4②는 개념/흐름 그림을 반드시 mermaid로 그리고 ASCII 박스 다이어그램을 금지한다. → [1]~[4] 순서도를 mermaid flowchart 블록으로 변환하고 흑백 init 지시자를 붙인다.
- **MED** `실습재현불가` _문제 17 풀이 bash 코드 블록 (line 215)_ — sleep 15 명령으로 Pod 기동 대기를 처리한다. CLAUDE.md §7에서 sleep 연쇄 대신 kubectl wait 또는 until 루프를 사용하도록 명시한다. 또한 Multi-Container Pod 풀이에 검증 스크린샷이 없어 학생이 실습 성공 여부를 확인할 수 없다. → sleep 15를 kubectl wait --for=condition=Ready pod/multi-pod -n demo --timeout=60s 로 대체하고 kubectl logs 결과를 보여주는 스크린샷을 추가한다.
- **LOW** `용어비약` _문제 23 Headless Service 풀이 (Headless 다이어그램 h4 노드)_ — StatefulSet: Pod별 DNS 엔트리(예: postgres-0.db-headless.demo.svc.cluster.local)라는 설명이 등장하지만, StatefulSet이 무엇인지, 왜 Headless Service와 결합해 Pod별 DNS를 생성하는지에 대한 설명이 없다. Day 20은 종합 모의시험이라 StatefulSet을 처음 접하는 학생은 이 노드를 이해하지 못한다. → h4 노드 아래 1문장 각주(StatefulSet은 Pod마다 고정 이름을 부여하는 리소스이며 Headless Service와 결합해 각 Pod에 안정적인 DNS 주소를 제공한다)를 추가하거나 mermaid 노드 캡션에 간단한 설명을 삽입한다.
- **LOW** `맥락점프` _실습 2 (line 984-987)_ — 예상 출력 설명 텍스트('예상 출력 (예시 -- fresh dev 클러스터에는...')가 스크린샷 이미지(day20-09-dev-summary.png) 바로 앞에 위치해, 이 이미지가 실제 실측인지 예시 참조인지 모호하다. 설명에서 '실제 출력 형태는 day11~16의 dev 실측에서 검증했다'고 언급하나 링크가 없다. → 스크린샷이 실제 실측 캡처라면 캡션을 '실제 터미널 캡처'로 수정하고 전제 조건 텍스트를 주석 처리한다. 예시 참조라면 이미지 위에 '(예시 참조)'를 명시해 CLAUDE.md §4① 위반 여부를 명확히 구분한다.
